---
phase: 09-test-infrastructure-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Contributors can run tests in same environment as CI"
    - "Contributors understand why Docker is needed (font consistency)"
    - "Contributors know how to update baseline snapshots after intentional CSS changes"
    - "Contributors can interpret CI test failures and find artifacts"
  artifacts:
    - path: "README.md"
      provides: "Testing section with Docker instructions"
      contains: "docker compose -f docker-compose.test.yml"
  key_links:
    - from: "README.md"
      to: "docker-compose.test.yml"
      via: "command example"
      pattern: "docker compose -f docker-compose.test.yml"
    - from: "README.md"
      to: ".github/workflows/pdf-tests.yml"
      via: "CI explanation"
      pattern: "CI|GitHub Actions|PR"
---

<objective>
Document test infrastructure for contributors.

Purpose: Enable contributors to run tests locally in the same Docker environment as CI, update baseline snapshots, and debug visual regression failures.

Output: README.md with a new "Development" section containing testing documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-infrastructure-foundation/09-VERIFICATION.md

# Reference files to document
@docker-compose.test.yml
@Dockerfile.test
@.github/workflows/pdf-tests.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add testing documentation to README.md</name>
  <files>README.md</files>
  <action>
Add a "Development" section to README.md before the "License" section.

The section must include:

1. **Running Tests** subsection:
   - Explain WHY Docker is needed (font rendering consistency across environments)
   - Command to run tests locally: `docker compose -f docker-compose.test.yml run --rm test`
   - What tests include (visual regression for 3 templates, page count assertions, PDF metadata validation)

2. **Updating Baseline Snapshots** subsection:
   - When to update (after intentional CSS changes that affect visual output)
   - Command: `docker compose -f docker-compose.test.yml run --rm test npx playwright test --update-snapshots`
   - Note about reviewing diffs before committing

3. **CI** subsection:
   - Tests run automatically on every PR via GitHub Actions
   - On failure, CI uploads artifacts: snapshot diffs and Playwright report
   - How to find artifacts: Actions tab -> failed run -> Artifacts section

Keep documentation concise. Focus on actionable instructions, not implementation details.
Do NOT add emojis.
  </action>
  <verify>
    - Run: `grep -q "docker compose -f docker-compose.test.yml" README.md && echo "Docker command found"`
    - Run: `grep -q "update-snapshots" README.md && echo "Snapshot update command found"`
    - Run: `grep -q "GitHub Actions\|CI" README.md && echo "CI mention found"`
  </verify>
  <done>
    - README.md contains Development section with testing documentation
    - Docker test command documented
    - Snapshot update command documented
    - CI workflow explained
    - All content above License section
  </done>
</task>

</tasks>

<verification>
1. README.md contains all four gap items from 09-VERIFICATION.md:
   - Docker-based testing explanation
   - docker-compose.test.yml usage
   - CI workflow explanation
   - Snapshot management instructions

2. Documentation is actionable (commands can be copy-pasted)

3. Content is positioned in logical location (Development section before License)
</verification>

<success_criteria>
- Success criterion #4 from Phase 9 is satisfied: "Test infrastructure documented in project README or dedicated test docs"
- Contributors can run tests locally with a single command
- Contributors understand Docker requirement
- Contributors can update snapshots after CSS changes
- Contributors can debug CI failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-infrastructure-foundation/09-04-SUMMARY.md`
</output>
