---
phase: 09-test-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.test
  - docker-compose.test.yml
  - .github/workflows/pdf-tests.yml
autonomous: true

must_haves:
  truths:
    - "Tests can run inside Docker container with deterministic results"
    - "CI pipeline triggers on PRs and main branch pushes"
    - "Font rendering is consistent between local Docker and CI"
  artifacts:
    - path: "Dockerfile.test"
      provides: "Docker image definition for test environment"
      contains: "oven/bun"
    - path: "docker-compose.test.yml"
      provides: "Docker Compose configuration for running tests"
      contains: "cvgen-test"
    - path: ".github/workflows/pdf-tests.yml"
      provides: "GitHub Actions workflow for CI"
      contains: "docker"
  key_links:
    - from: ".github/workflows/pdf-tests.yml"
      to: "Dockerfile.test"
      via: "docker build"
      pattern: "docker build.*Dockerfile\\.test"
    - from: "docker-compose.test.yml"
      to: "Dockerfile.test"
      via: "build context"
      pattern: "dockerfile.*Dockerfile\\.test"
---

<objective>
Create Docker-based CI infrastructure for consistent PDF testing across environments.

Purpose: Establish deterministic test environment that eliminates font rendering differences between local development and CI, satisfying TEST-06 (tests run in CI with consistent environment).

Output: Dockerfile.test, docker-compose.test.yml, and GitHub Actions workflow that runs PDF tests on every PR.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-infrastructure-foundation/09-RESEARCH.md

# Existing config
@package.json
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker test environment</name>
  <files>Dockerfile.test, docker-compose.test.yml</files>
  <action>
Create Dockerfile.test based on oven/bun:latest with:
- Chromium installation for Puppeteer (chromium package, not chromium-browser)
- Font packages for consistent rendering (fonts-liberation, fonts-freefont-ttf, fonts-noto-cjk)
- Environment variables: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true, PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
- Working directory /app with package.json and bun.lock copied first (layer caching)
- Full project copy and bun install --frozen-lockfile
- Default CMD: bun test

Create docker-compose.test.yml with:
- Service named "test" building from Dockerfile.test
- init: true for proper signal handling
- cap_add: SYS_ADMIN for Chromium sandboxing
- Volume mount for local development (optional, commented out)
- Network mode: host for simplicity

Do NOT use chromium-browser (does not exist in Debian). Use "chromium" package.
  </action>
  <verify>
Run: `docker compose -f docker-compose.test.yml build` completes without errors
Run: `docker compose -f docker-compose.test.yml run --rm test chromium --version` outputs version
  </verify>
  <done>Docker image builds successfully and Chromium is available at /usr/bin/chromium</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions workflow</name>
  <files>.github/workflows/pdf-tests.yml</files>
  <action>
Create .github/workflows/pdf-tests.yml with:
- Trigger on: pull_request, push to main branch
- Single job "test" running on ubuntu-latest
- Steps:
  1. actions/checkout@v4
  2. Build test container: docker build -t cvgen-test -f Dockerfile.test .
  3. Run tests: docker run --rm --init --cap-add=SYS_ADMIN cvgen-test bun test
  4. Upload playwright-report as artifact on failure
  5. Upload snapshot diffs as artifact on failure (tests/__snapshots__/**/*-diff.png)

Set retention-days: 7 for artifacts.
Use if: failure() condition for artifact uploads.
  </action>
  <verify>
Validate YAML syntax: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pdf-tests.yml'))"`
Check workflow structure includes required steps
  </verify>
  <done>GitHub Actions workflow file exists with correct trigger conditions and Docker-based test execution</done>
</task>

<task type="auto">
  <name>Task 3: Update Playwright config for Docker compatibility</name>
  <files>playwright.config.ts</files>
  <action>
Update playwright.config.ts to:
- Set fullyParallel: false (PDF generation should be sequential)
- Set workers: 1 (single worker for consistent PDF generation)
- Add expect.toHaveScreenshot config:
  - maxDiffPixelRatio: 0.01 (1% tolerance for anti-aliasing)
  - animations: 'disabled'
- Add launchOptions.args with '--font-render-hinting=none' for deterministic rendering
- Keep existing projects array with chromium
- Add reporter: [['html', { open: 'never' }], ['junit', { outputFile: 'test-results/junit.xml' }]]

Preserve existing baseURL: 'file://' and trace settings.
  </action>
  <verify>
TypeScript compiles: `bunx tsc --noEmit playwright.config.ts`
Config exports valid defineConfig
  </verify>
  <done>Playwright config updated with Docker-compatible settings and visual regression thresholds</done>
</task>

</tasks>

<verification>
1. Docker build succeeds: `docker compose -f docker-compose.test.yml build`
2. Chromium available in container: `docker compose -f docker-compose.test.yml run --rm test chromium --version`
3. GitHub Actions workflow valid YAML
4. Playwright config TypeScript valid
</verification>

<success_criteria>
- Dockerfile.test creates consistent test environment with Bun + Chromium + fonts
- docker-compose.test.yml provides easy local test execution
- GitHub Actions workflow triggers on PR and main, runs tests in Docker
- Playwright config optimized for PDF visual regression testing
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-infrastructure-foundation/09-01-SUMMARY.md`
</output>
