---
phase: 09-test-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/sample-cv.md
  - tests/fixtures/multi-page-cv.md
  - tests/helpers/pdf-utils.ts
  - tests/helpers/test-generator.ts
autonomous: true

must_haves:
  truths:
    - "Test fixture CV data exists with realistic IT professional content"
    - "Multi-page fixture generates 2+ pages for pagination testing"
    - "PDF can be generated from fixture data using helper functions"
    - "PDF page count can be retrieved programmatically"
  artifacts:
    - path: "tests/fixtures/sample-cv.md"
      provides: "Single-page CV test data"
      contains: "work_history"
    - path: "tests/fixtures/multi-page-cv.md"
      provides: "Multi-page CV test data for pagination"
      min_lines: 150
    - path: "tests/helpers/pdf-utils.ts"
      provides: "PDF metadata utilities"
      exports: ["getPdfPageCount", "getPdfMetadata"]
    - path: "tests/helpers/test-generator.ts"
      provides: "CV generation for tests"
      exports: ["generateTestCv"]
  key_links:
    - from: "tests/helpers/test-generator.ts"
      to: "packages/cli/src/commands/build.ts"
      via: "CLI build function import"
      pattern: "import.*build|buildCommand"
    - from: "tests/helpers/pdf-utils.ts"
      to: "pdf-lib"
      via: "PDFDocument import"
      pattern: "PDFDocument.*pdf-lib"
---

<objective>
Create test fixtures and helper utilities for PDF testing infrastructure.

Purpose: Provide reusable test data and utilities that all PDF tests can use, enabling consistent testing of all three templates with both single-page and multi-page content.

Output: CV fixture files in Markdown format and TypeScript helper modules for PDF generation and metadata extraction.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-infrastructure-foundation/09-RESEARCH.md

# Existing CV structure
@examples/alex-chen/cv.md
@packages/core/src/types/cv.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixture CV files</name>
  <files>tests/fixtures/sample-cv.md, tests/fixtures/multi-page-cv.md</files>
  <action>
Create tests/fixtures/ directory if it doesn't exist.

Create sample-cv.md with:
- Frontmatter: name "Test User", title "Senior Software Engineer", locale "en"
- Contact section: email, phone, location, linkedin, github (use example.com domains)
- Summary: 2-3 sentences
- Work history: 2 jobs with 3-4 bullet points each
- Education: 1 degree
- Skills: 8-10 technical skills
- Total length: should fit on 1 page with Modern template

Create multi-page-cv.md with:
- Same frontmatter structure, name "Multi Page Test"
- Contact section with all fields
- Longer summary: 4-5 sentences
- Work history: 5 jobs with 5-6 bullet points each (detailed achievements)
- Education: 2 degrees with descriptions
- Skills: 20+ technical skills organized by category
- Certifications: 3-4 certifications
- Projects: 2-3 side projects
- Total length: should generate 2-3 pages with any template

Use realistic IT professional content (not lorem ipsum). Include dates that span multiple years.
Do NOT include dynamic dates (use fixed dates like "2020-01 to 2023-06").
  </action>
  <verify>
Files exist: `ls tests/fixtures/sample-cv.md tests/fixtures/multi-page-cv.md`
Frontmatter valid: `head -20 tests/fixtures/sample-cv.md` shows YAML frontmatter
Content substantial: `wc -l tests/fixtures/multi-page-cv.md` shows 150+ lines
  </verify>
  <done>Two test fixture files exist with realistic CV content, one single-page and one multi-page</done>
</task>

<task type="auto">
  <name>Task 2: Create PDF utilities helper</name>
  <files>tests/helpers/pdf-utils.ts</files>
  <action>
Create tests/helpers/ directory if it doesn't exist.

Create pdf-utils.ts with:

1. getPdfPageCount(pdfPath: string): Promise<number>
   - Read PDF file using Bun.file()
   - Parse with PDFDocument from pdf-lib
   - Return page count

2. getPdfMetadata(pdfPath: string): Promise<PdfMetadata>
   - Return object with: pageCount, title, author, fileSize
   - Use pdf-lib for metadata extraction

3. getPdfFileSize(pdfPath: string): Promise<number>
   - Return file size in bytes

4. Type definitions:
   - PdfMetadata interface
   - Export all functions and types

Import PDFDocument from 'pdf-lib' (already installed in project).
Use async/await pattern throughout.
  </action>
  <verify>
TypeScript compiles: `bunx tsc --noEmit tests/helpers/pdf-utils.ts`
Exports correct: File contains export statements for all functions
  </verify>
  <done>PDF utilities module provides page count, metadata, and file size extraction</done>
</task>

<task type="auto">
  <name>Task 3: Create test CV generator helper</name>
  <files>tests/helpers/test-generator.ts</files>
  <action>
Create test-generator.ts with:

1. generateTestCv(options: TestCvOptions): Promise<TestCvResult>
   - options: { template: 'modern' | 'minimal' | 'classic', fixture: 'sample-cv' | 'multi-page-cv' }
   - Creates temporary output directory if needed
   - Copies fixture to temp location
   - Runs CV generation using the CLI build command or direct API
   - Returns paths to generated HTML, PDF, DOCX

2. cleanupTestOutput(): Promise<void>
   - Removes temporary test output directory

3. Type definitions:
   - TestCvOptions interface
   - TestCvResult interface with html, pdf, docx paths

The generator should:
- Use the existing build command from packages/cli/src/commands/build.ts
- Output to tests/output/{fixture}_{template}/ directory
- Handle all three output formats

Import from relative path: '../../packages/cli/src/commands/build' or use the CLI programmatically.
  </action>
  <verify>
TypeScript compiles: `bunx tsc --noEmit tests/helpers/test-generator.ts`
Exports correct: File exports generateTestCv and cleanupTestOutput
  </verify>
  <done>Test generator helper can produce CV outputs for any template and fixture combination</done>
</task>

</tasks>

<verification>
1. Fixture files exist with proper content
2. All TypeScript files compile without errors
3. Helpers export expected functions
4. No hardcoded dynamic content (dates, timestamps) in fixtures
</verification>

<success_criteria>
- tests/fixtures/ contains sample-cv.md (single page) and multi-page-cv.md (2-3 pages)
- tests/helpers/pdf-utils.ts provides PDF metadata extraction
- tests/helpers/test-generator.ts provides CV generation for tests
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-infrastructure-foundation/09-02-SUMMARY.md`
</output>
