---
phase: 08-multi-template-polish
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - README.md
  - docs/MARKDOWN.md
  - docs/CLI.md
  - docs/CUSTOMIZATION.md
  - docs/FAQ.md
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "README.md has quick-start section that gets users running in 3 commands"
    - "README.md shows template comparison with descriptions"
    - "CLI.md documents all commands with flags and examples"
    - "MARKDOWN.md specifies CV format with all sections and syntax"
    - "CUSTOMIZATION.md explains config cascade and style options"
  artifacts:
    - path: "README.md"
      provides: "Project documentation and quick-start"
      contains: "Quick Start"
    - path: "docs/CLI.md"
      provides: "CLI command reference"
      contains: "cvgen build"
    - path: "docs/MARKDOWN.md"
      provides: "CV markdown format specification"
      contains: "## Summary"
    - path: "docs/CUSTOMIZATION.md"
      provides: "Template customization guide"
      contains: "config.json"
  key_links:
    - from: "README.md"
      to: "docs/CLI.md"
      via: "documentation link"
      pattern: "\\[CLI Reference\\]"
    - from: "README.md"
      to: "docs/CUSTOMIZATION.md"
      via: "documentation link"
      pattern: "\\[Template Customization\\]"
---

<objective>
Create comprehensive documentation including README.md with quick-start and reference, plus detailed docs in /docs/ directory.

Purpose: Enable users to install, use, and customize cvgen without reading source code. Good documentation is essential for adoption.

Output: README.md at project root, documentation files in /docs/ directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-template-polish/08-CONTEXT.md
@.planning/phases/08-multi-template-polish/08-RESEARCH.md
@.planning/phases/08-multi-template-polish/08-02-SUMMARY.md
@.planning/phases/08-multi-template-polish/08-03-SUMMARY.md
@.planning/phases/08-multi-template-polish/08-04-SUMMARY.md
@packages/cli/src/cli.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive README.md</name>
  <files>README.md</files>
  <action>
Replace the placeholder README with comprehensive documentation:

```markdown
# CV Generator

Generate ATS-optimized CVs from markdown.

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

## Features

- **ATS-Optimized**: Single-column layouts, standard fonts, semantic HTML structure
- **Multiple Formats**: Generate HTML, PDF, and DOCX from a single markdown source
- **Three Templates**: Modern (bold/geometric), Minimal (clean/spacious), Classic (traditional/formal)
- **Multi-Language**: Support for English and German section headers and date formats
- **Customizable**: Override colors, fonts, and margins via configuration
- **IT Professional**: Projects, certifications, and tech stack per job position

## Quick Start

```bash
# Install globally
bun add -g @gottz/cvgen

# Create a new CV
cvgen init john-doe

# Edit your CV
cd people/john-doe
# Edit cv.md with your information

# Generate all formats
cvgen build john-doe modern
```

Your CV will be generated in `people/john-doe/output/`:
- `john-doe_modern.html` - Self-contained HTML with embedded CSS
- `john-doe_modern.pdf` - Print-ready PDF with bookmarks
- `john-doe_modern.docx` - Word document with proper heading styles

## Templates

| Template | Style | Best For |
|----------|-------|----------|
| **modern** | Bold, geometric, generous whitespace | Tech, creative roles |
| **minimal** | Clean, airy, wide margins for notes | Print-focused, interviews |
| **classic** | Traditional, formal, centered header | Corporate, finance, law |

All templates are ATS-compliant with single-column layouts and standard fonts.

## Commands

### `cvgen build <name> <template>`

Generate CV in all formats.

```bash
# Generate with specific template
cvgen build john-doe modern

# Generate specific formats only
cvgen build john-doe minimal --html --pdf

# Skip DOCX generation
cvgen build john-doe classic --no-docx

# Dry run (show what would be generated)
cvgen build john-doe modern --dry-run
```

### `cvgen init <name>`

Create a new CV directory with example content.

```bash
cvgen init jane-smith
```

### `cvgen validate <name>`

Check CV markdown without generating files.

```bash
cvgen validate john-doe
```

### `cvgen list-templates`

Show available templates.

```bash
cvgen list-templates
```

## Documentation

- [CV Markdown Format](./docs/MARKDOWN.md) - Syntax for writing your CV
- [CLI Reference](./docs/CLI.md) - Complete command documentation
- [Template Customization](./docs/CUSTOMIZATION.md) - Colors, fonts, and margins
- [FAQ](./docs/FAQ.md) - Frequently asked questions

## Requirements

- [Bun](https://bun.sh/) 1.0+
- Chrome/Chromium (for PDF generation via Puppeteer)

## License

MIT - See [LICENSE](./LICENSE)

---

Made with care for IT professionals who deserve better CVs.
```
  </action>
  <verify>
Verify README.md exists and contains key sections: `grep -E "Quick Start|Templates|Commands|Documentation" README.md`
  </verify>
  <done>
README.md exists with quick-start, template comparison, command overview, and documentation links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLI reference documentation</name>
  <files>docs/CLI.md</files>
  <action>
Create comprehensive CLI documentation:

```markdown
# CLI Reference

Complete documentation for the cvgen command-line interface.

## Global Options

```
--version, -V    Show version number
--help, -h       Show help for a command
```

## Commands

### `cvgen build <name> <template>`

Generate CV files in HTML, PDF, and DOCX formats.

**Arguments:**
- `<name>` - Person name (directory under `/people/`)
- `<template>` - Template to use (modern, minimal, classic, or 'auto')

**Options:**
| Flag | Description |
|------|-------------|
| `--html` | Generate HTML only |
| `--pdf` | Generate PDF only |
| `--docx` | Generate DOCX only |
| `--no-html` | Skip HTML generation |
| `--no-pdf` | Skip PDF generation |
| `--no-docx` | Skip DOCX generation |
| `--locale, -l` | Language code (en, de) |
| `--dry-run, -n` | Show what would be generated without writing files |
| `--quiet, -q` | Suppress progress output |
| `--json` | Output results as JSON |

**Examples:**

```bash
# Generate all formats with modern template
cvgen build john-doe modern

# Generate only PDF
cvgen build john-doe minimal --pdf

# Generate HTML and PDF, skip DOCX
cvgen build john-doe classic --no-docx

# German language output
cvgen build john-doe modern --locale de

# See what would be generated
cvgen build john-doe modern --dry-run

# Machine-readable output
cvgen build john-doe modern --json
```

**Output:**

Files are written to `/people/<name>/output/`:
- `<name>_<template>.html`
- `<name>_<template>.pdf`
- `<name>_<template>.docx`

### `cvgen init <name>`

Create a new CV directory with example content.

**Arguments:**
- `<name>` - Person name (will be slugified: "John Doe" -> "john-doe")

**Options:**
| Flag | Description |
|------|-------------|
| `--force, -f` | Overwrite existing directory |
| `--quiet, -q` | Suppress progress output |

**Examples:**

```bash
# Create new CV directory
cvgen init john-doe

# Create with spaces (automatically slugified)
cvgen init "Jane Smith"

# Overwrite existing
cvgen init john-doe --force
```

**Creates:**
```
people/
└── john-doe/
    ├── cv.md           # Example CV markdown
    └── images/
        └── photo.jpg   # Placeholder photo
```

### `cvgen validate <name>`

Check CV markdown structure without generating files.

**Arguments:**
- `<name>` - Person name to validate

**Options:**
| Flag | Description |
|------|-------------|
| `--quiet, -q` | Show only errors |
| `--json` | Output results as JSON |

**Examples:**

```bash
# Validate CV structure
cvgen validate john-doe

# Get validation result as JSON
cvgen validate john-doe --json
```

**Output:**

Shows validation summary with:
- Number of sections found
- Number of jobs, degrees, skill categories
- Available locales
- Any warnings (unknown sections, expired certifications)

### `cvgen list-templates`

Display available templates.

**Options:**
| Flag | Description |
|------|-------------|
| `--json` | Output as JSON |

**Examples:**

```bash
# Show templates in table format
cvgen list-templates

# Get templates as JSON
cvgen list-templates --json
```

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success |
| 1 | Error (validation failed, file not found, etc.) |
| 130 | User cancelled (Ctrl+C) |

## Environment Variables

| Variable | Description |
|----------|-------------|
| `CVGEN_ACCENT_COLOR` | Override accent color (#hex format) |
| `CVGEN_FONT_HEADING` | Override heading font family |
| `CVGEN_FONT_BODY` | Override body font family |
| `CVGEN_MARGINS` | Override page margins (narrow/normal/wide or mm) |

Example:
```bash
CVGEN_ACCENT_COLOR="#dc2626" cvgen build john-doe modern
```
```
  </action>
  <verify>
Verify docs/CLI.md exists and contains all commands: `grep -E "cvgen build|cvgen init|cvgen validate|cvgen list-templates" docs/CLI.md`
  </verify>
  <done>
docs/CLI.md exists with complete command reference including all flags, examples, and environment variables.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create markdown format specification</name>
  <files>docs/MARKDOWN.md</files>
  <action>
Create CV markdown format documentation:

```markdown
# CV Markdown Format

This document specifies the markdown format for cvgen CV files.

## File Structure

CV files are named `cv.md` and located in `/people/<name>/cv.md`.

```markdown
---
name: John Doe
email: john@example.com
phone: +1 234 567 8900
location: San Francisco, CA
links:
  - type: linkedin
    url: https://linkedin.com/in/johndoe
  - type: github
    url: https://github.com/johndoe
---

## Summary `en`

Your professional summary goes here...

## Experience `en`

### Senior Developer at Tech Corp
*2020-01 - present | San Francisco, CA*

- Led development of microservices architecture
- Reduced deployment time by 60%

#### Technologies
- Kubernetes
- TypeScript
- PostgreSQL

---

### Developer at StartupCo
*2018-06 - 2019-12 | New York, NY*

...
```

## Frontmatter

The YAML frontmatter contains contact information:

| Field | Required | Description |
|-------|----------|-------------|
| `name` | Yes | Full name |
| `email` | No | Email address |
| `phone` | No | Phone number |
| `location` | No | City, State/Country |
| `links` | No | Array of {type, url, label?} |
| `photo` | No | Path to photo image |

### Links

```yaml
links:
  - type: linkedin
    url: https://linkedin.com/in/johndoe
  - type: github
    url: https://github.com/johndoe
    label: "GitHub Profile"  # Optional custom label
  - type: portfolio
    url: https://johndoe.dev
```

## Sections

Sections start with `## Section Name` and optionally include a language tag:

```markdown
## Experience `en`
## Berufserfahrung `de`
```

### Supported Sections

| English | German | Description |
|---------|--------|-------------|
| Summary | Zusammenfassung | Professional summary |
| Experience | Berufserfahrung | Work history |
| Education | Ausbildung | Education history |
| Skills | Kenntnisse | Skills by category |
| Projects | Projekte | Portfolio projects |
| Certifications | Zertifizierungen | Professional certifications |

### Summary

Plain text or markdown. No special formatting required.

```markdown
## Summary `en`

Results-driven software engineer with 8+ years of experience...
```

### Experience

Each job is a level-3 heading with metadata line:

```markdown
## Experience `en`

### Job Title at Company Name
*start-date - end-date | location*

- Achievement bullet point
- Another achievement

#### Technologies
- Tech 1
- Tech 2

---

### Previous Job Title at Other Company
...
```

**Date formats:** `YYYY-MM`, `YYYY`, or `present`

### Education

Similar to experience:

```markdown
## Education `en`

### Degree Name, Field of Study
*start-date - end-date*

Institution Name
Location (optional)

Honors or GPA (optional)

---
```

### Skills

Skills are grouped by category:

```markdown
## Skills `en`

### Programming Languages
- TypeScript (Expert)
- Python
- Go (Proficient)

### Cloud & Infrastructure
- AWS (Certified)
- Kubernetes (K8s)
- Terraform
```

Proficiency levels in parentheses are optional.

### Projects

```markdown
## Projects `en`

### Project Name
*2023-01 - present | Lead Developer | open-source*

Project description here.

#### Technologies
- React
- Node.js

#### Links
- github: https://github.com/user/project
- demo: https://project.example.com

**Outcome:** Achieved X improvement in Y metric

---
```

The `highlight: true` tag in metadata promotes a project to the top.

### Certifications

```markdown
## Certifications

### AWS Solutions Architect - Associate
*Amazon Web Services | 2023-05 | expires 2026-05*

Credential ID: ABC123XYZ
https://verify.aws.com/ABC123XYZ

---
```

Certifications are NOT localized (certification names are universal).

## Entry Separator

Use `---` (three dashes) to separate multiple entries within a section.

## Images

Store images in `/people/<name>/images/`:

```markdown
photo: ./images/headshot.jpg
```

Supported formats: JPEG, PNG, WebP

**Note:** Images are embedded in HTML (base64) and DOCX. A warning is displayed since ATS systems cannot parse image content.

## Multi-Language Support

Include sections for each language:

```markdown
## Summary `en`
English summary...

## Zusammenfassung `de`
German summary...

## Experience `en`
...

## Berufserfahrung `de`
...
```

Use `--locale` flag to generate for a specific language.
```
  </action>
  <verify>
Verify docs/MARKDOWN.md exists and contains section documentation: `grep -E "Experience|Education|Skills|Projects|Certifications" docs/MARKDOWN.md`
  </verify>
  <done>
docs/MARKDOWN.md exists with complete CV format specification including all sections, syntax examples, and multi-language support.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create customization guide and remaining docs</name>
  <files>
    docs/CUSTOMIZATION.md
    docs/FAQ.md
    docs/CHANGELOG.md
  </files>
  <action>
Create remaining documentation files:

**docs/CUSTOMIZATION.md:**
```markdown
# Template Customization

Customize template appearance via configuration files.

## Configuration Cascade

Configuration is merged from multiple sources (lowest to highest priority):

1. **Template defaults** - Built into template CSS
2. **Global config** - `/config.json` in project root
3. **Template config** - `/templates/<name>/config.json`
4. **Environment variables** - `CVGEN_*` variables
5. **Frontmatter** - `style` field in cv.md

Higher priority values override lower ones.

## Configuration Options

### Accent Color

Primary color used for links, dividers, and highlights.

```json
{
  "style": {
    "accentColor": "#2563eb"
  }
}
```

Must be hex format: `#RRGGBB`

### Fonts

Override heading and body font families:

```json
{
  "style": {
    "fontHeading": "'Times New Roman', Times, serif",
    "fontBody": "Arial, Helvetica, sans-serif"
  }
}
```

**Important:** Use web-safe fonts for ATS compatibility:
- Arial, Helvetica, sans-serif
- 'Times New Roman', Times, serif
- Calibri (Windows)

### Margins

Page margins as named size or millimeters:

```json
{
  "style": {
    "margins": "wide"
  }
}
```

| Name | Value |
|------|-------|
| `narrow` | 15mm |
| `normal` | 20mm |
| `wide` | 30mm |

Or specify exact value: `"margins": 25` (interpreted as mm)

## Global Configuration

Create `/config.json` in project root:

```json
{
  "style": {
    "accentColor": "#dc2626",
    "fontBody": "Calibri, Arial, sans-serif"
  }
}
```

Applies to all templates unless overridden.

## Per-Template Configuration

Edit `/templates/<name>/config.json`:

```json
{
  "$schema": "../../schemas/template-config.schema.json",
  "name": "Modern",
  "description": "Bold, geometric design",
  "style": {
    "accentColor": "#2563eb",
    "margins": "wide"
  }
}
```

## Per-Person Configuration

Add `style` field to cv.md frontmatter:

```yaml
---
name: John Doe
email: john@example.com
style:
  accentColor: "#059669"
---
```

## Environment Variables

Override any config via environment:

```bash
export CVGEN_ACCENT_COLOR="#7c3aed"
export CVGEN_MARGINS="narrow"
cvgen build john-doe modern
```

| Variable | Description |
|----------|-------------|
| `CVGEN_ACCENT_COLOR` | Primary accent color |
| `CVGEN_FONT_HEADING` | Heading font family |
| `CVGEN_FONT_BODY` | Body font family |
| `CVGEN_MARGINS` | Page margins |

## Creating Custom Templates

1. Copy an existing template:
   ```bash
   cp -r templates/modern templates/my-template
   ```

2. Edit `config.json`:
   ```json
   {
     "name": "My Template",
     "description": "Custom design"
   }
   ```

3. Modify `styles.css` with your custom styling

4. (Optional) Modify `template.njk` for structural changes

Templates must maintain ATS compliance:
- Single-column layout
- Standard fonts
- Semantic HTML (h1 for name, h2 for sections)
- No tables for layout
```

**docs/FAQ.md:**
```markdown
# Frequently Asked Questions

## General

### What is ATS and why does it matter?

ATS (Applicant Tracking System) is software that scans resumes before human review. Many companies use ATS systems like iCIMS, Workday, or Greenhouse.

CVs that aren't ATS-optimized may be rejected or incorrectly parsed, losing information. cvgen ensures your CV is machine-readable while looking professional.

### Which template should I use?

- **Modern**: Tech companies, startups, creative roles
- **Minimal**: Traditional companies, print-focused (interviews with notes)
- **Classic**: Finance, law, consulting, corporate environments

All three are ATS-compliant.

## Technical

### Why is Puppeteer required?

Puppeteer controls a headless Chrome browser to convert HTML to PDF. This ensures high-quality PDFs with proper fonts and layout.

Chrome/Chromium must be installed. Puppeteer will attempt to download it automatically.

### Can I use custom fonts?

Yes, but with caveats:

1. **For HTML**: Any web font works
2. **For PDF**: Font must be installed on the system
3. **For DOCX**: Use standard fonts (Arial, Times, Calibri)

For ATS compatibility, stick to standard fonts.

### Why are my images showing a warning?

ATS systems cannot parse image content. The warning reminds you that any text in images (like your photo) won't be searchable.

This is informational only - images are still included in output.

## Troubleshooting

### "Template not found"

Check available templates:
```bash
cvgen list-templates
```

Template names are case-sensitive: `modern`, `minimal`, `classic`.

### "Person not found"

Ensure the directory exists under `/people/`:
```bash
ls people/
```

Names are slugified: "John Doe" becomes `john-doe`.

### PDF generation fails

1. Ensure Chrome/Chromium is installed
2. Check for error messages about Puppeteer
3. Try running: `npx puppeteer browsers install chrome`

### DOCX looks different from PDF

Some CSS features don't translate to Word format. The DOCX generator extracts styles but cannot replicate:
- Gradients and shadows
- Complex borders
- Custom fonts (uses Arial/Times fallback)

This is expected behavior.
```

**docs/CHANGELOG.md:**
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Three template themes: Modern, Minimal, Classic
- Configuration cascade (global, template, env, frontmatter)
- Customizable colors, fonts, and margins
- Comprehensive documentation

## [0.1.0] - 2026-01-23

### Added
- Initial release
- HTML, PDF, DOCX output formats
- Multi-language support (EN, DE)
- Projects and Certifications sections
- Tech stack per job position
- CLI commands: build, init, validate, list-templates
- Base template with ATS-optimized layout
```
  </action>
  <verify>
Verify all docs exist:
- `ls docs/CUSTOMIZATION.md docs/FAQ.md docs/CHANGELOG.md`
- `grep "Configuration Cascade" docs/CUSTOMIZATION.md`
- `grep "ATS" docs/FAQ.md`
  </verify>
  <done>
docs/CUSTOMIZATION.md, docs/FAQ.md, and docs/CHANGELOG.md exist with comprehensive content.
  </done>
</task>

</tasks>

<verification>
1. README.md exists with quick-start and template comparison
2. docs/CLI.md documents all commands with examples
3. docs/MARKDOWN.md specifies complete CV format
4. docs/CUSTOMIZATION.md explains config cascade
5. docs/FAQ.md answers common questions
6. docs/CHANGELOG.md has initial release notes
7. All markdown files are valid and well-formatted
</verification>

<success_criteria>
- README.md enables users to start using cvgen in under 2 minutes
- CLI.md documents every command, flag, and environment variable
- MARKDOWN.md is a complete reference for CV format
- CUSTOMIZATION.md explains all configuration options
- Documentation links work (relative paths are correct)
- No placeholder or TODO content remains
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-template-polish/08-06-SUMMARY.md`
</output>
