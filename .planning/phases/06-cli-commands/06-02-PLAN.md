---
phase: 06-cli-commands
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/src/lib/scaffolder.ts
  - packages/cli/src/index.ts
autonomous: true

must_haves:
  truths:
    - "User can scaffold a new CV directory with example markdown"
    - "Init creates commented guide with inline comments explaining each section"
    - "Init creates placeholder photo image (solid color with PHOTO text)"
    - "Init prompts for overwrite/skip/cancel when directory exists"
    - "Directory naming converts 'John Doe' to 'john-doe'"
  artifacts:
    - path: "packages/cli/src/commands/init.ts"
      provides: "Init command action function"
      exports: ["initAction", "InitOptions"]
    - path: "packages/cli/src/lib/scaffolder.ts"
      provides: "Directory and file creation utilities"
      exports: ["createCvDirectory", "createExampleMarkdown", "createPlaceholderPhoto"]
    - path: "packages/cli/src/index.ts"
      provides: "Commander program with init command"
      contains: ".command('init')"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/commands/init.ts"
      via: "import initAction"
      pattern: "initAction"
    - from: "packages/cli/src/commands/init.ts"
      to: "packages/cli/src/lib/scaffolder.ts"
      via: "import scaffolder functions"
      pattern: "createCvDirectory|createExampleMarkdown"
---

<objective>
Implement the `init` command that scaffolds a new CV directory with example markdown and placeholder image.

Purpose: Users can quickly start a new CV without manually creating directory structure.
Output: Working `cvgen init <name>` command per CLI-02 requirement.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-commands/06-CONTEXT.md
@.planning/phases/06-cli-commands/06-RESEARCH.md
@packages/cli/src/index.ts
@packages/cli/src/commands/build.ts
@packages/cli/src/lib/console.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scaffolder utility module</name>
  <files>packages/cli/src/lib/scaffolder.ts</files>
  <action>
Create packages/cli/src/lib/scaffolder.ts with:

1. Import sharp: `import sharp from 'sharp'`
   Import mkdir, writeFile: `import { mkdir, writeFile } from 'node:fs/promises'`
   Import path: `import path from 'node:path'`

2. slugifyName(name: string): string
   - Per CONTEXT.md: "John Doe" -> "john-doe"
   - toLowerCase(), trim(), replace non-alphanumeric with hyphen, trim hyphens
   ```typescript
   return name.toLowerCase().trim()
     .replace(/[^a-z0-9]+/g, '-')
     .replace(/^-|-$/g, '');
   ```

3. createPlaceholderPhoto(): Promise<Buffer>
   - Per CONTEXT.md: solid color square with "PHOTO" text
   - Use sharp({ create: {...} }) pattern from RESEARCH.md Pattern 5
   - Size: 200x250 pixels (portrait ratio for CV photo)
   - Background: #CCCCCC (light gray)
   - Text: "PHOTO" in #666666, centered
   - Use SVG overlay for text:
   ```typescript
   const svg = `<svg width="200" height="250">
     <rect width="100%" height="100%" fill="#CCCCCC"/>
     <text x="50%" y="50%" font-family="sans-serif" font-size="24"
           fill="#666666" text-anchor="middle" dominant-baseline="middle">PHOTO</text>
   </svg>`;
   ```
   - Return JPEG buffer (quality 90)

4. createExampleMarkdown(): string
   - Per CONTEXT.md: Commented guide with inline comments, EN and DE examples
   - Include all core sections: summary, experience, education, skills
   - Format:
   ```markdown
   ---
   # Contact Information (required)
   name: Your Name
   email: your.email@example.com
   # phone: +1 234 567 8900  # Optional
   # location: City, Country  # Optional
   # slug: your-name  # Optional: used for output filenames
   # photo: ./images/photo.jpg  # Optional: your photo

   # Links (optional array)
   # links:
   #   - url: https://linkedin.com/in/yourprofile
   #     label: LinkedIn
   #   - url: https://github.com/yourusername
   #     label: GitHub
   ---

   ## Summary \`en\`
   <!-- Professional summary in English (2-3 sentences) -->
   Experienced professional with expertise in...

   ## Zusammenfassung \`de\`
   <!-- Berufliche Zusammenfassung auf Deutsch (2-3 Saetze) -->
   Erfahrener Fachmann mit Expertise in...

   ## Experience \`en\`
   <!-- Work experience entries. Each entry starts with ### -->

   ### Job Title at Company Name
   *January 2020 - Present | City, Country*

   - Key achievement or responsibility
   - Another accomplishment with measurable results
   - Technical skills or leadership demonstrated

   ---

   ### Previous Role at Another Company
   *June 2017 - December 2019 | City, Country*

   - Description of responsibilities
   - Notable projects or achievements

   ## Berufserfahrung \`de\`
   <!-- Berufserfahrung auf Deutsch -->

   ### Position bei Firmenname
   *Januar 2020 - Heute | Stadt, Land*

   - Wichtige Leistung oder Verantwortung
   - Weitere Erfolge mit messbaren Ergebnissen

   ## Education \`en\`
   <!-- Education entries -->

   ### Degree Name
   *University Name | 2013 - 2017*

   Field of Study
   <!-- Optional: honors, GPA, thesis topic -->

   ## Ausbildung \`de\`

   ### Abschlussbezeichnung
   *Universitaet Name | 2013 - 2017*

   Studienfach

   ## Skills \`en\`
   <!-- Skills organized by category -->

   ### Languages
   - JavaScript
   - TypeScript
   - Python

   ### Frameworks
   - React
   - Node.js
   - Express

   ### Tools
   - Git
   - Docker
   - AWS

   ## Kenntnisse \`de\`

   ### Sprachen
   - JavaScript
   - TypeScript
   - Python

   ### Frameworks
   - React
   - Node.js
   - Express
   ```

5. createCvDirectory(personDir: string): Promise<void>
   - Create personDir directory (recursive: true)
   - Create personDir/images subdirectory
   - Write cv.md with example markdown
   - Write images/photo.jpg with placeholder
   - Per CONTEXT.md: Do NOT create output directory (build creates it)

6. Export: slugifyName, createPlaceholderPhoto, createExampleMarkdown, createCvDirectory
  </action>
  <verify>Run `bun run typecheck` from workspace root - should pass</verify>
  <done>scaffolder.ts provides all utilities for CV directory scaffolding</done>
</task>

<task type="auto">
  <name>Task 2: Create init command action</name>
  <files>packages/cli/src/commands/init.ts</files>
  <action>
Create packages/cli/src/commands/init.ts with:

1. Imports:
   - path from 'node:path'
   - { access, constants } from 'node:fs/promises' (for checking directory existence)
   - { createConsole } from '../lib/console.ts'
   - { slugifyName, createCvDirectory } from '../lib/scaffolder.ts'
   - { promptOverwrite, promptName } from '../lib/prompts.ts'

2. InitOptions interface:
   - quiet?: boolean
   - json?: boolean

3. Exit code constant:
   - EXIT_USER_CANCELLED = 130 (per RESEARCH.md convention)

4. initAction(nameArg: string | undefined, options: InitOptions): Promise<void>

   a. Create console with options

   b. Determine name:
      - If nameArg provided, use slugifyName(nameArg)
      - If not provided: per CONTEXT.md interactive mode, call promptName()
      - If promptName returns empty, show error "Name is required" and exit

   c. Build personDir path:
      - cwd/people/{slugified-name}/

   d. Check if directory exists:
      - Use access(personDir, constants.F_OK) wrapped in try/catch
      - If exists: call promptOverwrite(personDir)
        - 'overwrite': continue (will overwrite files)
        - 'skip': cons.info("Skipping..."), return
        - 'cancel': cons.info("Cancelled"), process.exit(EXIT_USER_CANCELLED)

   e. Call createCvDirectory(personDir)

   f. Success output:
      - cons.success(`Created ${path.relative(process.cwd(), personDir)}/`)
      - cons.info(`  cv.md - Edit this file with your CV content`)
      - cons.info(`  images/photo.jpg - Replace with your photo`)
      - cons.info(``)
      - cons.info(`Next: cvgen build ${name} base`)

5. Export initAction and InitOptions
  </action>
  <verify>Run `bun run typecheck` from workspace root - should pass</verify>
  <done>init.ts exports initAction that scaffolds CV directory with proper prompts</done>
</task>

<task type="auto">
  <name>Task 3: Register init command in CLI entry point</name>
  <files>packages/cli/src/index.ts</files>
  <action>
Update packages/cli/src/index.ts:

1. Add import for initAction:
   ```typescript
   import { initAction } from './commands/init.ts';
   ```

2. Add init command after build command:
   ```typescript
   // Init command
   program
     .command('init')
     .description('Scaffold a new CV directory with example content')
     .argument('[name]', 'Person name (e.g., john-doe or "John Doe")')
     .option('--quiet', 'Suppress non-error output')
     .option('--json', 'Output results as JSON')
     .addHelpText('after', `
   Examples:
     $ cvgen init john-doe
     $ cvgen init "John Doe"
     $ cvgen init              # Interactive: prompts for name
   `)
     .action(initAction);
   ```

Per CONTEXT.md: Help examples shown directly in --help output (addHelpText).
  </action>
  <verify>Run `cd packages/cli && bun run src/index.ts init --help` - should show init command help with examples</verify>
  <done>Init command registered in CLI with proper options and help text</done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes (no TypeScript errors)
2. `cvgen init --help` shows command description and examples
3. `cvgen init test-person` creates people/test-person/ with cv.md and images/photo.jpg
4. cv.md contains commented example with EN and DE sections
5. images/photo.jpg is a valid JPEG with placeholder content
6. Running `cvgen init test-person` again prompts for overwrite/skip/cancel
</verification>

<success_criteria>
1. `cvgen init john-doe` creates people/john-doe/ directory
2. cv.md contains inline comments explaining each section
3. images/photo.jpg exists as placeholder
4. Directory naming converts spaces to hyphens
5. Existing directory prompts for user choice
</success_criteria>

<output>
After completion, create `.planning/phases/06-cli-commands/06-02-SUMMARY.md`
</output>
