---
phase: 17-ai-user-control
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - packages/cli/src/ai/review/review-session.ts
  - packages/cli/src/ai/review/index.ts
autonomous: true

must_haves:
  truths:
    - "User reviews suggestions one at a time"
    - "Accept, edit, skip, regenerate actions work correctly"
    - "Accept-all and skip-all bulk actions work"
    - "Weak bullets flagged with inline warnings during review"
    - "Review session tracks accepted, skipped, and edited items"
  artifacts:
    - path: "packages/cli/src/ai/review/review-session.ts"
      provides: "Main review loop orchestrating all review actions"
      exports: ["runReviewSession", "ReviewState", "ReviewItem"]
      min_lines: 100
  key_links:
    - from: "packages/cli/src/ai/review/review-session.ts"
      to: "packages/cli/src/ai/review/review-prompt.ts"
      via: "promptReviewAction"
      pattern: "promptReviewAction"
    - from: "packages/cli/src/ai/review/review-session.ts"
      to: "packages/cli/src/ai/review/editor-integration.ts"
      via: "openInEditor"
      pattern: "openInEditor"
    - from: "packages/cli/src/ai/review/review-session.ts"
      to: "packages/cli/src/ai/review/weak-bullet-detector.ts"
      via: "detectWeakness and displayWeakBulletWarning"
      pattern: "detectWeakness"
    - from: "packages/cli/src/ai/review/review-session.ts"
      to: "packages/cli/src/ai/display/diff-display.ts"
      via: "displayComparison"
      pattern: "displayComparison"
---

<objective>
Create the main review session loop that orchestrates all review actions.

Purpose: Core implementation for AI-11, AI-12, AI-13 (preview, diff view, individual control).
Output: Review session module that ties together prompts, editor, regeneration, and detection.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ai-user-control/17-CONTEXT.md
@.planning/phases/17-ai-user-control/17-RESEARCH.md
@.planning/phases/17-ai-user-control/17-01-SUMMARY.md
@.planning/phases/17-ai-user-control/17-02-SUMMARY.md
@.planning/phases/17-ai-user-control/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create review session orchestrator</name>
  <files>
    packages/cli/src/ai/review/review-session.ts
    packages/cli/src/ai/review/index.ts
  </files>
  <action>
1. Create `packages/cli/src/ai/review/review-session.ts`:
   - Import pc from 'picocolors'
   - Import displayComparison from '../display/diff-display.ts'
   - Import from local modules:
     - promptReviewAction from './review-prompt.ts'
     - openInEditor from './editor-integration.ts'
     - regenerateWithGuidance, RegenerationAttempt from './regeneration.ts'
     - detectWeakness, displayWeakBulletWarning from './weak-bullet-detector.ts'

   - Define `ReviewItem` interface:
     ```typescript
     interface ReviewItem {
       section: string;
       original: string;
       suggested: string;
       weaknessType?: string;
       // For regeneration context
       generateFn?: (guidance: string, temperature: number) => Promise<string>;
     }
     ```

   - Define `ReviewState` interface:
     ```typescript
     interface ReviewState {
       accepted: ReviewItem[];
       skipped: ReviewItem[];
       edited: Map<ReviewItem, string>; // item -> edited content
     }
     ```

   - Create `async runReviewSession(items: ReviewItem[]): Promise<ReviewState>`:
     - Initialize state: { accepted: [], skipped: [], edited: new Map() }
     - Initialize per-item regeneration history: Map<ReviewItem, RegenerationAttempt[]>

     - Loop through items (use while loop with index for regeneration):
       - const item = items[i]
       - const remaining = items.length - i

       - Display context header:
         - `\n${pc.dim(`[${item.section}]`)} (${i + 1}/${items.length})`

       - Check and display weakness warning:
         - const weakness = item.weaknessType || detectWeakness(item.original)
         - if (weakness) displayWeakBulletWarning(item.original, weakness)

       - Display diff:
         - Get current suggestion (may have been regenerated)
         - console.log(displayComparison(item.original, currentSuggestion))

       - Prompt for action:
         - const { action } = await promptReviewAction({ section: item.section, index: i + 1, total: items.length })

       - Handle action with switch:
         - 'accept':
           - Push item to state.accepted (with currentSuggestion if regenerated)
           - i++
         - 'skip':
           - Push item to state.skipped
           - i++
         - 'edit':
           - const result = openInEditor(item.original, currentSuggestion)
           - if (!result.cancelled && result.content):
             - state.edited.set(item, result.content)
           - i++ (move forward even if cancelled)
         - 'regenerate':
           - if (!item.generateFn):
             - console.log(pc.yellow('Regeneration not available for this item'))
             - continue
           - Get or initialize history for this item
           - const { newSuggestion, history } = await regenerateWithGuidance({
               original: item.original,
               currentSuggestion,
               history: itemHistory,
               generateFn: item.generateFn
             })
           - Update history map
           - Update currentSuggestion (store in separate map or modify item)
           - Don't increment i (stay on same item)
         - 'accept_all':
           - for (let j = i; j < items.length; j++):
             - state.accepted.push(items[j])
           - i = items.length (exit loop)
         - 'skip_all':
           - for (let j = i; j < items.length; j++):
             - state.skipped.push(items[j])
           - i = items.length (exit loop)

     - Return state

2. Update `packages/cli/src/ai/review/index.ts`:
   - Add exports from review-session.ts: runReviewSession, ReviewState, ReviewItem
  </action>
  <verify>
    - `bun run typecheck` passes
    - `bun run lint` passes
    - ReviewState and ReviewItem types exported
    - runReviewSession function exported
  </verify>
  <done>
    - Review session processes items one at a time
    - All six actions (accept, edit, skip, regenerate, accept_all, skip_all) handled
    - Weak bullets flagged with inline warnings
    - Regeneration integrates with history and similarity detection
    - State tracks accepted, skipped, and edited items
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run lint` passes
3. `grep -r "runReviewSession" packages/cli/src/ai/review/review-session.ts` finds function
4. `grep -r "displayComparison" packages/cli/src/ai/review/review-session.ts` finds diff display usage
5. `grep -r "detectWeakness" packages/cli/src/ai/review/review-session.ts` finds weakness detection
6. `wc -l packages/cli/src/ai/review/review-session.ts` shows >= 100 lines
</verification>

<success_criteria>
- Review session orchestrates all review actions
- Weak bullet detection integrated into review flow
- Diff display shown for every comparison
- All actions properly tracked in state
- All new code passes typecheck and lint
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-user-control/17-04-SUMMARY.md`
</output>
