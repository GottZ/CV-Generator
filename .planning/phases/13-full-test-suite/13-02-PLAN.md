---
phase: 13-full-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/structural.spec.ts
autonomous: true

must_haves:
  truths:
    - "PDF page count can be verified against expectations"
    - "PDF metadata (title, author) is set on generated documents"
    - "PDF file size is within reasonable bounds (not empty, not bloated)"
  artifacts:
    - path: "tests/structural.spec.ts"
      provides: "Centralized structural tests for PDF quality"
      min_lines: 80
  key_links:
    - from: "tests/structural.spec.ts"
      to: "tests/helpers/pdf-utils.ts"
      via: "import getPdfMetadata"
      pattern: "import.*getPdfMetadata.*from.*pdf-utils"
    - from: "tests/structural.spec.ts"
      to: "tests/helpers/test-generator.ts"
      via: "import generateTestCv"
      pattern: "import.*generateTestCv.*from.*test-generator"
---

<objective>
Create centralized structural tests that verify PDF page count, metadata presence, and file size sanity across all templates and fixtures.

Purpose: Ensures generated PDFs meet structural quality requirements - correct page count, proper metadata for document management, and reasonable file sizes that won't cause upload/storage issues.

Output: Comprehensive structural test suite covering TEST-03, TEST-04, and TEST-05 requirements.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/helpers/pdf-utils.ts
@tests/helpers/test-generator.ts
@tests/fixtures/sample-cv.md
@tests/fixtures/multi-page-cv.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structural test suite for page count verification (TEST-03)</name>
  <files>tests/structural.spec.ts</files>
  <action>
Create `tests/structural.spec.ts` with comprehensive structural tests:

1. Import dependencies:
   - `test, expect` from `@playwright/test`
   - `getPdfMetadata` from `./helpers/pdf-utils`
   - `generateTestCv, cleanupTestOutput, type TestCvResult` from `./helpers/test-generator`

2. Create describe block 'PDF Structural Tests':
   - Configure as serial mode
   - beforeAll: Generate CVs for all template/fixture combinations:
     - modern + sample-cv
     - modern + multi-page-cv
     - minimal + sample-cv
     - classic + sample-cv
   - afterAll: cleanupTestOutput()

3. TEST-03: Page Count Tests - Create nested describe 'Page Count Expectations':

   **Test: 'sample CV produces 1-2 pages'**
   - For each template (modern, minimal, classic) with sample-cv
   - Assert pageCount >= 1 and <= 2
   - Short CV should fit on 1-2 pages

   **Test: 'multi-page CV produces 2+ pages'**
   - For modern template with multi-page-cv
   - Assert pageCount >= 2
   - Long CV must produce multiple pages (validates pagination CSS works)

   **Test: 'page count is reasonable upper bound'**
   - Assert pageCount <= 10 for any CV
   - Sanity check: CV should never be 10+ pages

Use `test.describe.configure({ mode: 'serial' })` for the outer block.
  </action>
  <verify>Tests compile: `bunx tsc --noEmit tests/structural.spec.ts`</verify>
  <done>Page count tests defined for sample and multi-page CVs</done>
</task>

<task type="auto">
  <name>Task 2: Add metadata and file size tests (TEST-04, TEST-05)</name>
  <files>tests/structural.spec.ts</files>
  <action>
Extend `tests/structural.spec.ts` with metadata and file size tests:

1. TEST-04: Metadata Presence - Create nested describe 'PDF Metadata':

   **Test: 'PDF has title metadata'**
   - Get metadata from generated PDF
   - Assert title is defined (not undefined)
   - Assert title is non-empty string
   - Note: Title format may vary; just verify presence

   **Test: 'PDF has author metadata'**
   - Assert author is defined
   - Assert author contains expected name or is non-empty
   - Note: Author should be set by pdf-generator.ts

2. TEST-05: File Size Sanity - Create nested describe 'File Size Sanity':

   **Test: 'PDF is not empty (> 10KB)'**
   - Get fileSize from metadata
   - Assert fileSize > 10_000 bytes (10KB minimum)
   - Empty or near-empty PDFs indicate generation failure

   **Test: 'PDF is not bloated (< 5MB)'**
   - Assert fileSize < 5_000_000 bytes (5MB maximum)
   - Bloated PDFs indicate embedded resources issues

   **Test: 'multi-page PDF is reasonably larger'**
   - Compare multi-page CV size to single-page
   - Multi-page should be larger (more content)
   - But not 10x larger (would indicate problem)

3. Add summary test at the end:

   **Test: 'all templates produce valid PDFs'**
   - Loop through all generated results
   - Verify each has valid metadata (pageCount > 0, fileSize > 0)
   - This catches any template-specific generation issues
  </action>
  <verify>Run all structural tests: `bunx playwright test structural.spec.ts --reporter=list`</verify>
  <done>All structural tests pass (page count, metadata, file size)</done>
</task>

</tasks>

<verification>
1. `tests/structural.spec.ts` exists with 80+ lines
2. Tests cover TEST-03 (page count), TEST-04 (metadata), TEST-05 (file size)
3. All tests pass: `bunx playwright test structural.spec.ts`
4. Tests exercise all 3 templates (modern, minimal, classic)
5. Tests exercise both fixtures (sample-cv, multi-page-cv)
</verification>

<success_criteria>
- TEST-03 satisfied: Page count tests verify expectations (1-2 for short, 2+ for long)
- TEST-04 satisfied: Metadata tests verify title and author presence
- TEST-05 satisfied: File size tests verify reasonable bounds (10KB-5MB)
- Tests are maintainable with clear structure and descriptive names
- Tests work in Docker CI environment
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-test-suite/13-02-SUMMARY.md`
</output>
