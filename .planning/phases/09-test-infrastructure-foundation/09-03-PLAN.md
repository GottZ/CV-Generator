---
phase: 09-test-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - tests/pdf-modern.spec.ts
  - tests/pdf-minimal.spec.ts
  - tests/pdf-classic.spec.ts
  - tests/__snapshots__/pdf-modern.spec.ts-snapshots/
  - tests/__snapshots__/pdf-minimal.spec.ts-snapshots/
  - tests/__snapshots__/pdf-classic.spec.ts-snapshots/
autonomous: true

must_haves:
  truths:
    - "Visual regression test exists for Modern template"
    - "Visual regression test exists for Minimal template"
    - "Visual regression test exists for Classic template"
    - "Baseline snapshots generated inside Docker for consistency"
    - "Tests verify page count matches expectations"
  artifacts:
    - path: "tests/pdf-modern.spec.ts"
      provides: "Modern template visual regression tests"
      contains: "toHaveScreenshot"
    - path: "tests/pdf-minimal.spec.ts"
      provides: "Minimal template visual regression tests"
      contains: "toHaveScreenshot"
    - path: "tests/pdf-classic.spec.ts"
      provides: "Classic template visual regression tests"
      contains: "toHaveScreenshot"
    - path: "tests/__snapshots__/"
      provides: "Baseline PNG snapshots for all templates"
  key_links:
    - from: "tests/pdf-modern.spec.ts"
      to: "tests/helpers/test-generator.ts"
      via: "generateTestCv import"
      pattern: "import.*generateTestCv"
    - from: "tests/pdf-modern.spec.ts"
      to: "tests/helpers/pdf-utils.ts"
      via: "getPdfPageCount import"
      pattern: "import.*getPdfPageCount"
---

<objective>
Create visual regression tests and generate baseline snapshots for all three CV templates.

Purpose: Establish baseline visual snapshots that enable regression detection throughout milestone v1.1, satisfying TEST-07 (baseline snapshots exist for all 3 templates).

Output: Playwright test files for Modern, Minimal, and Classic templates with baseline PNG snapshots stored in version control.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-test-infrastructure-foundation/09-RESEARCH.md

# Depends on Plan 01 and 02 outputs
@.planning/phases/09-test-infrastructure-foundation/09-01-SUMMARY.md
@.planning/phases/09-test-infrastructure-foundation/09-02-SUMMARY.md

# Existing test pattern
@tests/theme-toggle.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Modern template PDF tests</name>
  <files>tests/pdf-modern.spec.ts</files>
  <action>
Create pdf-modern.spec.ts using @playwright/test:

Import helpers: generateTestCv, getPdfPageCount from ./helpers/

Test suite "Modern template PDF snapshots":

1. Test "single page CV matches baseline":
   - Generate PDF using generateTestCv({ template: 'modern', fixture: 'sample-cv' })
   - Navigate to PDF file URL: await page.goto(`file://${pdfPath}`)
   - Wait for PDF to load: await page.waitForTimeout(1000)
   - Take screenshot: await expect(page).toHaveScreenshot('modern-single-page.png')

2. Test "multi-page CV matches baseline":
   - Generate PDF using generateTestCv({ template: 'modern', fixture: 'multi-page-cv' })
   - Get page count using getPdfPageCount()
   - Navigate to PDF
   - Screenshot each page with loop:
     - Screenshot: `modern-page-${i}.png`
     - Navigate next: await page.keyboard.press('PageDown')
     - Wait between pages: await page.waitForTimeout(500)

3. Test "page count matches expectations":
   - Single page CV should have exactly 1 page
   - Multi-page CV should have 2-3 pages (use toBeGreaterThanOrEqual(2))

Use test.describe.configure({ mode: 'serial' }) for sequential execution.
Add test.beforeAll to generate CVs once, store paths in shared variable.
  </action>
  <verify>
TypeScript compiles: `bunx tsc --noEmit tests/pdf-modern.spec.ts`
Test structure valid: File contains test.describe with 3 test cases
  </verify>
  <done>Modern template test file exists with visual regression and page count tests</done>
</task>

<task type="auto">
  <name>Task 2: Create Minimal and Classic template PDF tests</name>
  <files>tests/pdf-minimal.spec.ts, tests/pdf-classic.spec.ts</files>
  <action>
Create pdf-minimal.spec.ts following same pattern as Modern:
- Replace 'modern' with 'minimal' throughout
- Replace 'Modern' with 'Minimal' in descriptions
- Snapshot names: minimal-single-page.png, minimal-page-{i}.png

Create pdf-classic.spec.ts following same pattern:
- Replace 'modern' with 'classic' throughout
- Replace 'Modern' with 'Classic' in descriptions
- Snapshot names: classic-single-page.png, classic-page-{i}.png

Both files should:
- Import same helpers
- Use same test structure (describe, beforeAll, 3 tests)
- Configure serial mode
- Test both fixtures (sample-cv and multi-page-cv)
  </action>
  <verify>
TypeScript compiles: `bunx tsc --noEmit tests/pdf-minimal.spec.ts tests/pdf-classic.spec.ts`
All templates covered: `grep -l "toHaveScreenshot" tests/pdf-*.spec.ts | wc -l` equals 3
  </verify>
  <done>Minimal and Classic template test files exist with same test coverage as Modern</done>
</task>

<task type="auto">
  <name>Task 3: Generate baseline snapshots in Docker</name>
  <files>tests/__snapshots__/</files>
  <action>
Generate baseline snapshots using Docker for consistency:

1. Run: `docker compose -f docker-compose.test.yml run --rm test bunx playwright test --update-snapshots`

2. Verify snapshots created in tests/__snapshots__/ directory

3. Verify snapshot files for all templates:
   - pdf-modern.spec.ts-snapshots/*.png
   - pdf-minimal.spec.ts-snapshots/*.png
   - pdf-classic.spec.ts-snapshots/*.png

4. Add tests/__snapshots__/ to git tracking (not in .gitignore)

5. Verify snapshots are reasonable size (not empty, not huge):
   - Each PNG should be 50KB-2MB range

If Docker build fails, debug and fix Dockerfile.test issues first.
If tests fail to generate CVs, verify test fixtures and helpers work.
  </action>
  <verify>
Snapshots exist: `ls tests/__snapshots__/pdf-*.spec.ts-snapshots/*.png`
Files not empty: `find tests/__snapshots__ -name "*.png" -size +10k | wc -l` shows multiple files
Tests pass: `docker compose -f docker-compose.test.yml run --rm test bunx playwright test`
  </verify>
  <done>Baseline PNG snapshots exist for all 3 templates, generated in Docker environment</done>
</task>

</tasks>

<verification>
1. All three template test files exist and compile
2. Tests run successfully in Docker: `docker compose -f docker-compose.test.yml run --rm test bunx playwright test`
3. Baseline snapshots exist in tests/__snapshots__/
4. Snapshot files are tracked in git (not gitignored)
5. Each template has both single-page and multi-page baselines
</verification>

<success_criteria>
- tests/pdf-modern.spec.ts, tests/pdf-minimal.spec.ts, tests/pdf-classic.spec.ts exist
- Each test file has visual regression tests for single and multi-page CVs
- Each test file has page count assertion tests
- tests/__snapshots__/ contains baseline PNGs for all templates
- `bun test` (in Docker) passes with all baselines matching
</success_criteria>

<output>
After completion, create `.planning/phases/09-test-infrastructure-foundation/09-03-SUMMARY.md`
</output>
