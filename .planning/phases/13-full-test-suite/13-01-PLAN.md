---
phase: 13-full-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/helpers/text-extraction.ts
  - tests/text-extraction.spec.ts
autonomous: true

must_haves:
  truths:
    - "PDF text can be extracted and verified programmatically"
    - "ATS-critical content (name, title, experience) is readable from PDF"
    - "Ligature disabling works - words like 'efficient' extract correctly"
  artifacts:
    - path: "tests/helpers/text-extraction.ts"
      provides: "PDF text extraction utility using unpdf"
      exports: ["extractTextFromPdf", "TextExtractionResult"]
    - path: "tests/text-extraction.spec.ts"
      provides: "ATS text extraction verification tests"
      min_lines: 60
  key_links:
    - from: "tests/text-extraction.spec.ts"
      to: "tests/helpers/text-extraction.ts"
      via: "import extractTextFromPdf"
      pattern: "import.*extractTextFromPdf.*from.*text-extraction"
    - from: "tests/helpers/text-extraction.ts"
      to: "unpdf"
      via: "library import"
      pattern: "import.*from.*unpdf"
---

<objective>
Implement PDF text extraction tests that verify ATS (Applicant Tracking System) parsers can read CV content from generated PDFs.

Purpose: Ensures generated PDFs are machine-readable and content survives the markdown-to-PDF pipeline intact. This is critical for the project's core value of ATS-parseable CVs.

Output: Text extraction helper utility and test suite verifying PDF content readability.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-full-test-suite/13-RESEARCH.md
@tests/helpers/pdf-utils.ts
@tests/helpers/test-generator.ts
@tests/fixtures/sample-cv.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unpdf dependency and create text extraction helper</name>
  <files>package.json, tests/helpers/text-extraction.ts</files>
  <action>
1. Add unpdf as a dev dependency:
   ```bash
   bun add -d unpdf
   ```

2. Create `tests/helpers/text-extraction.ts` with:
   - Interface `TextExtractionResult` with fields: `totalPages: number`, `text: string`, `pageTexts: string[]`
   - Function `extractTextFromPdf(pdfPath: string): Promise<TextExtractionResult>`
   - Uses `getDocumentProxy` and `extractText` from unpdf
   - Reads PDF file with Node.js `readFile` (Playwright runs in Node)
   - Returns merged text and per-page text arrays

Follow the pattern from RESEARCH.md:
```typescript
import { extractText, getDocumentProxy } from 'unpdf';
import { readFile } from 'node:fs/promises';
```

Include JSDoc comments explaining purpose and usage examples.
  </action>
  <verify>TypeScript compiles: `bunx tsc --noEmit tests/helpers/text-extraction.ts`</verify>
  <done>text-extraction.ts exports extractTextFromPdf and TextExtractionResult</done>
</task>

<task type="auto">
  <name>Task 2: Create text extraction test suite</name>
  <files>tests/text-extraction.spec.ts</files>
  <action>
Create `tests/text-extraction.spec.ts` with Playwright test structure:

1. Import dependencies:
   - `@playwright/test` for test framework
   - `extractTextFromPdf` from helpers
   - `generateTestCv`, `cleanupTestOutput` from test-generator
   - `getPdfMetadata` from pdf-utils

2. Create test describe block 'ATS Text Extraction':
   - Configure as serial mode (tests share generated CVs)
   - Use beforeAll to generate test CVs (sample-cv with modern template)
   - Use afterAll to clean up

3. Test cases (each verifies TEST-02):

   **Test: 'extracts candidate name from PDF'**
   - Extract text from generated PDF
   - Assert text contains 'Test User' (from sample-cv.md fixture)

   **Test: 'extracts professional title/summary'**
   - Assert text contains 'Senior Software Engineer'
   - Assert text contains key summary phrases

   **Test: 'extracts work experience'**
   - Assert text contains company names: 'TechCorp Solutions', 'StartupXYZ'
   - Assert text contains job titles

   **Test: 'extracts skills correctly'**
   - Assert text contains 'TypeScript', 'React', 'PostgreSQL'

   **Test: 'ligature words extract correctly'**
   - Assert text contains 'efficient' (fi ligature test)
   - Assert text contains 'office' if present (ffi ligature)
   - These words should NOT be mangled by ligature glyphs

   **Test: 'extracts education section'**
   - Assert text contains 'University of Texas at Austin'
   - Assert text contains 'Bachelor of Science'

4. Use meaningful test names that describe the ATS requirement being verified.
  </action>
  <verify>Run tests: `bunx playwright test text-extraction.spec.ts --reporter=list`</verify>
  <done>All 6 text extraction tests pass, verifying ATS readability</done>
</task>

</tasks>

<verification>
1. unpdf is listed in package.json devDependencies
2. `tests/helpers/text-extraction.ts` exists and exports required functions
3. `tests/text-extraction.spec.ts` contains 6+ test cases
4. All tests pass: `bunx playwright test text-extraction.spec.ts`
5. Tests verify real content from fixture files (not placeholder assertions)
</verification>

<success_criteria>
- TEST-02 requirement satisfied: Text extraction tests verify ATS-readable content in PDFs
- PDF text includes candidate name, title, experience, education, skills
- Ligature disabling works (verified by extracting words with fi/fl/ff sequences)
- Tests run in Docker CI environment without issues
</success_criteria>

<output>
After completion, create `.planning/phases/13-full-test-suite/13-01-SUMMARY.md`
</output>
