---
phase: 20-template-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - packages/cli/src/template/prompts/colors.ts
  - packages/cli/src/template/prompts/fonts.ts
  - packages/cli/src/template/prompts/margins.ts
  - packages/cli/src/template/prompts/sections.ts
  - packages/cli/src/template/prompts/index.ts
autonomous: true

must_haves:
  truths:
    - "Color prompt allows preset selection or custom hex input"
    - "Font prompt restricts selection to ATS-safe fonts only"
    - "Margin prompt uses named sizes for simplicity"
    - "Section prompt uses checkbox multi-select for visibility"
    - "Custom hex color validates format before accepting"
  artifacts:
    - path: "packages/cli/src/template/prompts/colors.ts"
      provides: "selectAccentColor with preset + custom option"
      exports: ["selectAccentColor"]
    - path: "packages/cli/src/template/prompts/fonts.ts"
      provides: "selectFonts for heading and body"
      exports: ["selectFonts", "selectHeadingFont", "selectBodyFont"]
    - path: "packages/cli/src/template/prompts/margins.ts"
      provides: "selectMargins with named options"
      exports: ["selectMargins"]
    - path: "packages/cli/src/template/prompts/sections.ts"
      provides: "selectVisibleSections checkbox"
      exports: ["selectVisibleSections"]
    - path: "packages/cli/src/template/prompts/index.ts"
      provides: "Barrel export for prompts"
      min_lines: 5
  key_links:
    - from: "packages/cli/src/template/prompts/colors.ts"
      to: "@inquirer/prompts"
      via: "select + input for custom"
      pattern: "import.*select.*input.*from.*@inquirer/prompts"
    - from: "packages/cli/src/template/prompts/colors.ts"
      to: "packages/templates/src/config/colors.ts"
      via: "isValidHexColor for validation"
      pattern: "isValidHexColor"
---

<objective>
Create interactive prompts for template customization (colors, fonts, margins, sections).

Purpose: TPL-02, TPL-03, TPL-04, TPL-08 require users to customize templates through guided prompts. These prompts reuse @inquirer/prompts patterns established in Phase 18 wizard.

Output: Four prompt modules for color, font, margin, and section visibility selection.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-template-scaffolding/20-RESEARCH.md
@.planning/phases/20-template-scaffolding/20-02-SUMMARY.md

Reference existing patterns:
@packages/cli/src/wizard/menu.ts - @inquirer/prompts patterns, select/checkbox usage
@packages/cli/src/wizard/prompts/contact.ts - input validation patterns
@packages/templates/src/config/colors.ts - isValidHexColor function
@packages/cli/src/template/constants.ts - ATS_SAFE_FONTS, COLOR_PRESETS, MARGIN_OPTIONS
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create color selection prompt</name>
  <files>packages/cli/src/template/prompts/colors.ts</files>
  <action>
Create colors.ts with accent color selection prompt:

```typescript
/**
 * Color selection prompts for template customization.
 * Provides preset colors and custom hex input with validation.
 */

import { input, select } from '@inquirer/prompts';
import { isValidHexColor } from '@gottz/cv-templates';

import { COLOR_PRESETS } from '../constants.ts';

/**
 * Prompt user to select accent color for template.
 * Offers preset colors or custom hex input.
 * @returns Selected hex color (e.g., "#2563eb")
 */
export async function selectAccentColor(): Promise<string> {
  // Build choices from presets plus custom option
  const choices = [
    ...COLOR_PRESETS.map((preset) => ({
      value: preset.value,
      name: `${preset.name} ${preset.value}`,
    })),
    { value: 'custom', name: 'Enter custom hex color...' },
  ];

  const selection = await select({
    message: 'Select accent color:',
    choices,
    default: COLOR_PRESETS[0]?.value,
  });

  // Handle custom color input
  if (selection === 'custom') {
    return input({
      message: 'Enter hex color (e.g., #3b82f6):',
      validate: (value) => {
        const trimmed = value.trim();
        if (!trimmed) {
          return 'Color is required';
        }
        // Add # if missing
        const hex = trimmed.startsWith('#') ? trimmed : `#${trimmed}`;
        if (!isValidHexColor(hex)) {
          return 'Please enter a valid hex color (e.g., #3b82f6)';
        }
        return true;
      },
      transformer: (value) => {
        // Normalize to include #
        const trimmed = value.trim();
        return trimmed.startsWith('#') ? trimmed : `#${trimmed}`;
      },
    });
  }

  return selection;
}
```

Use existing COLOR_PRESETS from constants. Reuse isValidHexColor from cv-templates for validation.
Custom input allows with or without # prefix and normalizes output.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Color prompt shows presets and validates custom hex input. Uses existing isValidHexColor.</done>
</task>

<task type="auto">
  <name>Task 2: Create font selection prompts</name>
  <files>packages/cli/src/template/prompts/fonts.ts</files>
  <action>
Create fonts.ts with ATS-safe font selection:

```typescript
/**
 * Font selection prompts for template customization.
 * Restricts to ATS-safe fonts with proper fallback stacks.
 */

import { select } from '@inquirer/prompts';

import { ATS_SAFE_FONTS } from '../constants.ts';

/**
 * Prompt user to select heading font.
 * @returns Font stack string (e.g., "Arial, Helvetica, sans-serif")
 */
export async function selectHeadingFont(): Promise<string> {
  const choices = ATS_SAFE_FONTS.map((font) => ({
    value: font.value,
    name: font.name,
  }));

  return select({
    message: 'Select heading font:',
    choices,
    default: ATS_SAFE_FONTS[0]?.value,
  });
}

/**
 * Prompt user to select body text font.
 * @returns Font stack string (e.g., "Arial, Helvetica, sans-serif")
 */
export async function selectBodyFont(): Promise<string> {
  const choices = ATS_SAFE_FONTS.map((font) => ({
    value: font.value,
    name: font.name,
  }));

  return select({
    message: 'Select body font:',
    choices,
    default: ATS_SAFE_FONTS[0]?.value,
  });
}

/**
 * Combined font selection for heading and body.
 * Shows both prompts sequentially.
 * @returns Object with fontHeading and fontBody values
 */
export async function selectFonts(): Promise<{
  fontHeading: string;
  fontBody: string;
}> {
  const fontHeading = await selectHeadingFont();
  const fontBody = await selectBodyFont();

  return { fontHeading, fontBody };
}
```

No custom font input - per 20-RESEARCH.md Pitfall 1, only ATS-safe fonts allowed.
selectFonts provides convenience function for wizard flow.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Font prompts restrict to ATS-safe options. No custom input prevents ATS compatibility issues.</done>
</task>

<task type="auto">
  <name>Task 3: Create margin selection prompt</name>
  <files>packages/cli/src/template/prompts/margins.ts</files>
  <action>
Create margins.ts with named margin selection:

```typescript
/**
 * Margin selection prompt for template customization.
 * Uses named sizes for simplicity, maps to mm values in cv-templates.
 */

import { select } from '@inquirer/prompts';

import { MARGIN_OPTIONS } from '../constants.ts';

/**
 * Prompt user to select page margins.
 * Uses named sizes that cv-templates resolves to mm values.
 * @returns Margin name ('narrow' | 'normal' | 'wide')
 */
export async function selectMargins(): Promise<'narrow' | 'normal' | 'wide'> {
  const choices = MARGIN_OPTIONS.map((opt) => ({
    value: opt.value,
    name: opt.name,
  }));

  return select({
    message: 'Select page margins:',
    choices,
    default: 'normal',
  }) as Promise<'narrow' | 'normal' | 'wide'>;
}
```

Named margins only - cv-templates resolveMargin handles conversion to mm.
Type cast to narrow union type for downstream type safety.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Margin prompt uses named sizes. cv-templates handles mm conversion.</done>
</task>

<task type="auto">
  <name>Task 4: Create section visibility prompt</name>
  <files>packages/cli/src/template/prompts/sections.ts</files>
  <action>
Create sections.ts with checkbox multi-select for section visibility:

```typescript
/**
 * Section visibility prompt for template customization.
 * Allows users to show/hide optional CV sections.
 */

import { checkbox } from '@inquirer/prompts';

import type { SectionVisibility } from '../types.ts';

/**
 * Section options for visibility selection.
 * Only includes truly optional sections.
 */
const SECTION_OPTIONS = [
  { value: 'summary', name: 'Summary', checked: true },
  { value: 'projects', name: 'Projects', checked: true },
  { value: 'certifications', name: 'Certifications', checked: true },
] as const;

/**
 * Prompt user to select which optional sections to show.
 * Experience, Education, and Skills are always shown (required for ATS).
 * @returns SectionVisibility configuration
 */
export async function selectVisibleSections(): Promise<SectionVisibility> {
  const selections = await checkbox({
    message: 'Select sections to show (Experience, Education, Skills always shown):',
    choices: SECTION_OPTIONS.map((opt) => ({
      value: opt.value,
      name: opt.name,
      checked: opt.checked,
    })),
  });

  return {
    showSummary: selections.includes('summary'),
    showProjects: selections.includes('projects'),
    showCertifications: selections.includes('certifications'),
  };
}
```

Experience, Education, Skills are always required (ATS needs them).
Summary, Projects, Certifications are optional and can be hidden.
Default is all sections visible.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Section visibility uses checkbox. Core sections always shown. Optional sections configurable.</done>
</task>

<task type="auto">
  <name>Task 5: Create prompts barrel export</name>
  <files>packages/cli/src/template/prompts/index.ts</files>
  <action>
Create barrel export for prompts:

```typescript
/**
 * Template customization prompts.
 * Re-exports all prompt functions for wizard use.
 */

export { selectAccentColor } from './colors.ts';
export { selectFonts, selectHeadingFont, selectBodyFont } from './fonts.ts';
export { selectMargins } from './margins.ts';
export { selectVisibleSections } from './sections.ts';
```

Update template/index.ts to include prompts:

```typescript
// Add to existing exports
export {
  selectAccentColor,
  selectFonts,
  selectHeadingFont,
  selectBodyFont,
  selectMargins,
  selectVisibleSections,
} from './prompts/index.ts';
```
  </action>
  <verify>Imports work: `bun -e "import { selectAccentColor, selectFonts } from './packages/cli/src/template/index.ts'"`</verify>
  <done>All prompts accessible from template module. Clean import path for wizard.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd /workspace && bun run build`
2. Prompts import correctly: `bun -e "import * as p from './packages/cli/src/template/prompts/index.ts'; console.log(Object.keys(p))"`
3. Main barrel includes prompts: `bun -e "import { selectAccentColor } from './packages/cli/src/template/index.ts'"`
</verification>

<success_criteria>
1. selectAccentColor shows presets and validates custom hex
2. selectFonts only offers ATS-safe fonts (no custom input)
3. selectMargins uses named sizes
4. selectVisibleSections uses checkbox for optional sections
5. All prompts follow @inquirer/prompts patterns from Phase 18
6. Color validation uses existing isValidHexColor from cv-templates
</success_criteria>

<output>
After completion, create `.planning/phases/20-template-scaffolding/20-03-SUMMARY.md`
</output>
