---
phase: 18-wizard-foundation
plan: 08
type: execute
wave: 6
depends_on: ["18-06"]
files_modified:
  - packages/cli/src/wizard/menu.ts
  - packages/cli/src/wizard/runner.ts
  - packages/cli/src/wizard/prompts/contact.ts
  - packages/cli/src/wizard/prompts/projects.ts
  - packages/cli/src/wizard/prompts/skills.ts
  - packages/cli/src/wizard/prompts/experience.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All select menus have a Back/Cancel option"
    - "Empty input in 'add another' flows acts as back/cancel"
    - "Wizard prompts for locale at start"
  artifacts:
    - path: "packages/cli/src/wizard/menu.ts"
      provides: "selectLocale function for language selection"
      exports: ["selectLocale"]
    - path: "packages/cli/src/wizard/prompts/contact.ts"
      provides: "Back option in link type select, empty URL cancels"
      exports: ["collectSingleLink"]
---

<objective>
Fix UX gaps found during verification: add back navigation to select menus, handle empty input as cancel, and prompt for locale at wizard start.

Purpose: Complete the wizard UX requirements identified during human verification.
Output: Improved navigation flow with back options and locale selection.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-wizard-foundation/18-VERIFICATION.md
@packages/cli/src/wizard/menu.ts
@packages/cli/src/wizard/runner.ts
@packages/cli/src/wizard/prompts/contact.ts
@packages/cli/src/wizard/prompts/projects.ts
@packages/cli/src/wizard/prompts/skills.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add locale selection at wizard start</name>
  <files>packages/cli/src/wizard/menu.ts, packages/cli/src/wizard/runner.ts</files>
  <action>
Add locale prompt to wizard initialization:

**menu.ts:**
- Add selectLocale(): Promise<string>
  - Message: 'What language will this CV be in?'
  - Choices:
    - { value: 'en', name: 'English' }
    - { value: 'de', name: 'German (Deutsch)' }
    - { value: 'fr', name: 'French (Français)' }
    - { value: 'es', name: 'Spanish (Español)' }
    - { value: 'custom', name: 'Other...' }
  - If 'custom' selected, prompt for locale code
  - Default: 'en'

**runner.ts:**
- In runWizard(), after mode selection, call selectLocale()
- Store locale in options and pass to writeWizardOutput
- In runAddSection(), also prompt for locale (or read from existing CV frontmatter)

Export selectLocale from index.ts.
  </action>
  <verify>
bun run typecheck
bun run lint
  </verify>
  <done>
Wizard prompts for language at start
Locale passed through to markdown generation
  </done>
</task>

<task type="auto">
  <name>Task 2: Add back option to all select menus</name>
  <files>packages/cli/src/wizard/prompts/contact.ts, packages/cli/src/wizard/prompts/projects.ts, packages/cli/src/wizard/prompts/skills.ts</files>
  <action>
Add back/cancel option to select menus in "add another" flows:

**contact.ts - collectSingleLink():**
- Add to link type choices: { value: 'back', name: '← Back (cancel this link)' }
- If 'back' selected, return null
- Update collectLinks() to handle null return (skip adding to array)

**projects.ts - collectSingleProjectLink():**
- Same pattern: add 'back' option, return null if selected
- Update collectProjectLinks() to handle null

**skills.ts - collectSingleSkillCategory():**
- Add 'back' option to category selection (both quick and detailed mode)
- If 'back' selected, return null
- Update collectSkills() to handle null

**General pattern:**
```typescript
const BACK_CHOICE = { value: 'back', name: '← Back (cancel)' };

// In select:
choices: [
  BACK_CHOICE,
  new Separator(),
  ...otherChoices
]

// Handle:
if (selection === 'back') return null;
```

Update return types to include null where needed.
  </action>
  <verify>
bun run typecheck
bun run lint
  </verify>
  <done>
All select menus have back option
Selecting back cancels current entry
  </done>
</task>

<task type="auto">
  <name>Task 3: Empty input acts as cancel in add flows</name>
  <files>packages/cli/src/wizard/prompts/contact.ts, packages/cli/src/wizard/prompts/projects.ts</files>
  <action>
Make empty input cancel the current entry:

**contact.ts - collectSingleLink():**
- After selecting link type, prompt for URL
- If URL is empty, treat as cancel (return null)
- Show hint: '(empty to cancel)'

**projects.ts - collectSingleProjectLink():**
- Same pattern for URL input
- Empty URL = cancel = return null

**Pattern:**
```typescript
const url = await input({
  message: 'URL (empty to cancel):',
});

if (!url.trim()) {
  return null; // User cancelled
}
```

This provides two ways to back out:
1. Select "Back" from menu
2. Press Enter on empty URL
  </action>
  <verify>
bun run typecheck
bun run lint
  </verify>
  <done>
Empty URL cancels link entry
Provides quick escape from nested prompts
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run lint` passes
3. Wizard asks for locale at start
4. All select menus have back option
5. Empty URL in link prompts cancels entry
6. Back navigation doesn't corrupt state
</verification>

<success_criteria>
- Locale prompted at wizard start
- Back option available in all select menus
- Empty input in link URLs cancels the entry
- No state corruption when backing out
</success_criteria>

<output>
After completion, create `.planning/phases/18-wizard-foundation/18-08-SUMMARY.md`
</output>
