---
phase: 08-multi-template-polish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - templates/modern/config.json
  - templates/modern/template.njk
  - templates/modern/styles.css
autonomous: true

must_haves:
  truths:
    - "Modern template renders with bold geometric aesthetic"
    - "Modern template uses 3px accent-colored section dividers"
    - "Modern template has generous whitespace (25mm margins)"
    - "Modern template photo has rounded corners (12px border-radius)"
    - "Modern template is ATS-compliant (single-column, standard fonts)"
  artifacts:
    - path: "templates/modern/config.json"
      provides: "Modern template configuration"
      contains: "Modern"
    - path: "templates/modern/template.njk"
      provides: "Modern template markup"
      contains: "_shared/macros"
    - path: "templates/modern/styles.css"
      provides: "Modern template styles"
      contains: "--divider-width: 3px"
  key_links:
    - from: "templates/modern/template.njk"
      to: "templates/_shared/macros/entry.njk"
      via: "import macro"
      pattern: "from.*_shared/macros"
    - from: "templates/modern/styles.css"
      to: "CSS custom properties"
      via: "var() references"
      pattern: "var\\(--color-accent\\)"
---

<objective>
Create the Modern template with bold geometric design, high contrast colors, and generous whitespace.

Purpose: Provide a visually striking template suitable for tech and creative roles while maintaining ATS compliance.

Output: Complete Modern template in `/templates/modern/` with config.json, template.njk, and styles.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-template-polish/08-CONTEXT.md
@.planning/phases/08-multi-template-polish/08-RESEARCH.md
@.planning/phases/08-multi-template-polish/08-01-SUMMARY.md
@templates/base/template.njk
@templates/base/styles.css
@templates/_shared/macros/entry.njk
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Modern template config.json</name>
  <files>templates/modern/config.json</files>
  <action>
Create config.json for Modern template:

```json
{
  "$schema": "../../schemas/template-config.schema.json",
  "name": "Modern",
  "description": "Bold, geometric design with high contrast and generous whitespace. Ideal for tech and creative roles.",
  "atsCompliant": true,
  "singleColumn": true,
  "style": {
    "accentColor": "#2563eb",
    "fontHeading": "Arial, Helvetica, sans-serif",
    "fontBody": "Arial, Helvetica, sans-serif",
    "margins": "wide"
  }
}
```

The Modern template signature:
- Bold blue accent (#2563eb) - professional yet vibrant
- Sans-serif fonts throughout for clean readability
- Wide margins (30mm) for generous whitespace
- ATS-compliant single-column layout
  </action>
  <verify>
Verify file exists and is valid JSON: `cat templates/modern/config.json | jq .`
  </verify>
  <done>
Modern template config.json exists with name, description, ATS compliance flag, and style configuration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Modern template.njk</name>
  <files>templates/modern/template.njk</files>
  <action>
Create template.njk that imports shared macros and renders CV content with modern styling hooks:

```nunjucks
<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ contact.name }} - CV</title>
  <style>
    {{ css | safe }}
  </style>
</head>
<body class="modern">
  <div class="cv-page">
    {# Import shared macros #}
    {% from "../_shared/macros/entry.njk" import experienceEntry, educationEntry, projectEntry, certificationEntry %}

    {# Contact in body, not header - ATS-02 #}
    <header class="contact">
      <h1>{{ contact.name }}</h1>
      <div class="contact-info">
        {% if contact.email %}<span class="contact-item">{{ contact.email }}</span>{% endif %}
        {% if contact.phone %}<span class="contact-item">{{ contact.phone }}</span>{% endif %}
        {% if contact.location %}<span class="contact-item">{{ contact.location }}</span>{% endif %}
      </div>
      {% if contact.links and contact.links | length > 0 %}
      <div class="contact-links">
        {% for link in contact.links %}
        <a href="{{ link.url }}" class="contact-link">{{ link.label or link.type }}</a>
        {% endfor %}
      </div>
      {% endif %}
    </header>

    <main>
      {# Summary section #}
      {% if summary %}
      <section class="section summary">
        <h2>{{ 'summary' | sectionHeader(locale) }}</h2>
        <p class="summary-text">{{ summary | md | safe }}</p>
      </section>
      {% endif %}

      {# Work Experience section #}
      {% if experience and experience | length > 0 %}
      <section class="section experience">
        <h2>{{ 'experience' | sectionHeader(locale) }}</h2>
        {% for job in experience %}
        {{ experienceEntry(job, locale) }}
        {% endfor %}
      </section>
      {% endif %}

      {# Education section #}
      {% if education and education | length > 0 %}
      <section class="section education">
        <h2>{{ 'education' | sectionHeader(locale) }}</h2>
        {% for edu in education %}
        {{ educationEntry(edu, locale) }}
        {% endfor %}
      </section>
      {% endif %}

      {# Projects section #}
      {% if projects and projects | length > 0 %}
      <section class="section projects">
        <h2>{{ 'projects' | sectionHeader(locale) }}</h2>
        {% for project in projects %}
        {{ projectEntry(project, locale) }}
        {% endfor %}
      </section>
      {% endif %}

      {# Skills section #}
      {% if skills and skills | length > 0 %}
      <section class="section skills">
        <h2>{{ 'skills' | sectionHeader(locale) }}</h2>
        {% for category in skills %}
        <div class="skill-category">
          <h3 class="skill-category-name">{{ category.name }}</h3>
          <div class="skill-list">
            {% for skill in category.skills %}
            <span class="skill">{{ skill.name }}{% if skill.level %} ({{ skill.level }}){% endif %}</span>
            {% endfor %}
          </div>
        </div>
        {% endfor %}
      </section>
      {% endif %}

      {# Certifications section #}
      {% if certifications and certifications | length > 0 %}
      <section class="section certifications">
        <h2>{{ 'certifications' | sectionHeader(locale) }}</h2>
        {% for cert in certifications %}
        {{ certificationEntry(cert, locale) }}
        {% endfor %}
      </section>
      {% endif %}
    </main>
  </div>

  {# Theme toggle button - hidden in print #}
  <button class="theme-toggle" type="button" aria-label="Toggle color theme">
    <span class="theme-icon" data-icon="system">&#x2699;</span>
    <span class="theme-icon" data-icon="light">&#x2600;</span>
    <span class="theme-icon" data-icon="dark">&#x263D;</span>
  </button>

  <script>
    (function() {
      const toggle = document.querySelector('.theme-toggle');
      const html = document.documentElement;
      const STORAGE_KEY = 'cv-theme';
      const THEMES = ['system', 'light', 'dark'];

      function getStoredTheme() {
        try { return localStorage.getItem(STORAGE_KEY); } catch { return null; }
      }

      function setStoredTheme(theme) {
        try {
          if (theme === 'system') localStorage.removeItem(STORAGE_KEY);
          else localStorage.setItem(STORAGE_KEY, theme);
        } catch {}
      }

      function applyTheme(theme) {
        html.setAttribute('data-theme', theme);
        toggle.setAttribute('aria-label', {
          system: 'Current: system preference. Click to switch to light mode.',
          light: 'Current: light mode. Click to switch to dark mode.',
          dark: 'Current: dark mode. Click to switch to system preference.'
        }[theme] || 'Toggle theme');
      }

      function cycleTheme() {
        const current = html.getAttribute('data-theme') || 'system';
        const next = THEMES[(THEMES.indexOf(current) + 1) % THEMES.length];
        applyTheme(next);
        setStoredTheme(next);
      }

      const stored = getStoredTheme();
      applyTheme(stored && THEMES.includes(stored) ? stored : 'system');
      toggle.addEventListener('click', cycleTheme);
    })();
  </script>
</body>
</html>
```

Key differences from base:
- Imports and uses shared macros for entries
- Body class "modern" for CSS scoping
- Same structure but styled via modern CSS
  </action>
  <verify>
Verify file exists and contains macro imports: `grep -l "_shared/macros" templates/modern/template.njk`
  </verify>
  <done>
Modern template.njk exists, imports shared macros, and renders all CV sections with body class "modern".
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Modern styles.css</name>
  <files>templates/modern/styles.css</files>
  <action>
Create comprehensive styles.css for Modern template:

```css
/* CV Generator - Modern Template
 * Bold, geometric design with high contrast and generous whitespace
 * ATS-compliant: single-column, standard fonts, semantic structure
 */

/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  /* Modern: Sans-serif throughout, clean and bold */
  --font-heading: Arial, Helvetica, sans-serif;
  --font-body: Arial, Helvetica, sans-serif;
  --font-size-name: 28pt;
  --font-size-section: 14pt;
  --font-size-subsection: 12pt;
  --font-size-body: 11pt;
  --font-size-small: 10pt;

  /* Modern color palette - vibrant blue accent */
  --color-accent: #2563eb;
  --color-accent-light: #3b82f6;
  --color-heading: #0f172a;
  --color-body: #1e293b;
  --color-muted: #64748b;
  --color-border: #e2e8f0;
  --color-background: #ffffff;
  --color-surface: #f8fafc;

  /* Modern: Generous whitespace */
  --page-margin: 25mm;
  --spacing-xs: 6px;
  --spacing-sm: 10px;
  --spacing-md: 18px;
  --spacing-lg: 28px;
  --spacing-xl: 36px;

  /* Modern-specific: Bold dividers */
  --divider-color: var(--color-accent);
  --divider-width: 3px;

  /* Bullet style: filled circle */
  --bullet-char: '\2022';

  /* Page dimensions (A4) */
  --page-width: 210mm;
  --page-height: 297mm;
}

/* Dark mode - system preference */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    --color-accent: #60a5fa;
    --color-accent-light: #93c5fd;
    --color-heading: #f1f5f9;
    --color-body: #e2e8f0;
    --color-muted: #94a3b8;
    --color-border: #334155;
    --color-background: #0f172a;
    --color-surface: #1e293b;
  }

  :root:not([data-theme="light"]) body {
    background: #020617;
  }
}

/* Explicit dark mode */
[data-theme="dark"] {
  --color-accent: #60a5fa;
  --color-accent-light: #93c5fd;
  --color-heading: #f1f5f9;
  --color-body: #e2e8f0;
  --color-muted: #94a3b8;
  --color-border: #334155;
  --color-background: #0f172a;
  --color-surface: #1e293b;
}

[data-theme="dark"] body {
  background: #020617;
}

/* Explicit light mode */
[data-theme="light"] {
  --color-accent: #2563eb;
  --color-heading: #0f172a;
  --color-body: #1e293b;
  --color-muted: #64748b;
  --color-border: #e2e8f0;
  --color-background: #ffffff;
  --color-surface: #f8fafc;
}

[data-theme="light"] body {
  background: #f1f5f9;
}

body {
  font-family: var(--font-body);
  font-size: var(--font-size-body);
  color: var(--color-body);
  line-height: 1.6;
  background: #f1f5f9;
}

/* Page container */
.cv-page {
  width: var(--page-width);
  min-height: var(--page-height);
  padding: var(--page-margin);
  background: var(--color-background);
  margin: var(--spacing-lg) auto;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
}

/* Contact header - Modern: bold name with accent underline */
.contact {
  margin-bottom: var(--spacing-xl);
  padding-bottom: var(--spacing-lg);
  border-bottom: var(--divider-width) solid var(--color-accent);
}

h1 {
  font-family: var(--font-heading);
  font-size: var(--font-size-name);
  color: var(--color-heading);
  font-weight: 800;
  letter-spacing: -0.5px;
  margin-bottom: var(--spacing-sm);
}

.contact-info {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
  color: var(--color-muted);
  font-size: var(--font-size-body);
  margin-bottom: var(--spacing-xs);
}

.contact-item:not(:last-child)::after {
  content: '|';
  margin-left: var(--spacing-sm);
  color: var(--color-border);
}

.contact-links {
  margin-top: var(--spacing-sm);
}

.contact-link {
  color: var(--color-accent);
  text-decoration: none;
  margin-right: var(--spacing-md);
  font-size: var(--font-size-body);
  font-weight: 500;
}

.contact-link:hover {
  text-decoration: underline;
}

/* Single column layout */
main {
  display: flex;
  flex-direction: column;
}

/* Section headers - Modern: bold accent bar */
.section {
  margin-bottom: var(--spacing-lg);
}

h2 {
  font-family: var(--font-heading);
  font-size: var(--font-size-section);
  color: var(--color-heading);
  font-weight: 700;
  margin-bottom: var(--spacing-md);
  padding-bottom: var(--spacing-sm);
  border-bottom: var(--divider-width) solid var(--color-accent);
  text-transform: uppercase;
  letter-spacing: 1px;
}

/* Entry components */
.entry {
  margin-bottom: var(--spacing-md);
}

.entry:last-child {
  margin-bottom: 0;
}

.entry-header {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-xs);
}

h3 {
  font-family: var(--font-heading);
  font-size: var(--font-size-subsection);
  color: var(--color-heading);
  font-weight: 600;
}

.entry-title {
  flex: 1;
}

.date-range {
  color: var(--color-muted);
  font-size: var(--font-size-small);
  white-space: nowrap;
}

.entry-institution,
.entry-issuer {
  font-weight: 500;
  margin-bottom: var(--spacing-xs);
}

.entry-location {
  color: var(--color-muted);
  font-size: var(--font-size-small);
  margin-bottom: var(--spacing-xs);
}

.entry-honors {
  font-style: italic;
  color: var(--color-accent);
  font-size: var(--font-size-small);
}

.entry-notes {
  color: var(--color-body);
  font-size: var(--font-size-small);
  margin-top: var(--spacing-xs);
}

/* Bullet list */
.bullet-list {
  margin-top: var(--spacing-sm);
}

.bullet-item {
  padding-left: var(--spacing-md);
  position: relative;
  margin-bottom: var(--spacing-xs);
  line-height: 1.5;
}

.bullet-item::before {
  content: var(--bullet-char);
  position: absolute;
  left: 0;
  color: var(--color-accent);
  font-weight: bold;
}

.bullet-item:last-child {
  margin-bottom: 0;
}

/* Summary */
.summary-text {
  line-height: 1.7;
}

/* Skills */
.skill-category {
  margin-bottom: var(--spacing-md);
}

.skill-category:last-child {
  margin-bottom: 0;
}

.skill-category-name {
  font-size: var(--font-size-body);
  font-weight: 600;
  margin-bottom: var(--spacing-xs);
  color: var(--color-heading);
}

.skill-list {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
}

.skill {
  background: var(--color-surface);
  padding: 4px 12px;
  border-radius: 4px;
  font-size: var(--font-size-small);
  color: var(--color-body);
  border: 1px solid var(--color-border);
}

/* Tech stack tags */
.tech-stack {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
  margin-top: var(--spacing-sm);
}

.tech-tag {
  display: inline-block;
  padding: 3px 10px;
  background: var(--color-surface);
  color: var(--color-accent);
  font-size: var(--font-size-small);
  border-radius: 3px;
  font-weight: 500;
}

.job-tech-stack {
  margin-top: var(--spacing-md);
  padding-top: var(--spacing-sm);
  border-top: 1px solid var(--color-border);
}

/* Project styles */
.project-entry.highlighted {
  border-left: var(--divider-width) solid var(--color-accent);
  padding-left: var(--spacing-md);
  background: var(--color-surface);
  padding: var(--spacing-sm) var(--spacing-md);
  margin-left: calc(var(--divider-width) * -1);
}

.entry-meta {
  font-size: var(--font-size-small);
  color: var(--color-muted);
  margin-bottom: var(--spacing-xs);
}

.project-role::after {
  content: ' \2022 ';
}

.project-role:last-child::after {
  content: '';
}

.project-description {
  margin: var(--spacing-sm) 0;
}

.project-outcome {
  margin-top: var(--spacing-sm);
  padding: var(--spacing-sm) var(--spacing-md);
  background: var(--color-surface);
  border-left: 2px solid var(--color-accent);
}

.project-links {
  margin-top: var(--spacing-sm);
}

.project-link {
  color: var(--color-accent);
  text-decoration: none;
  margin-right: var(--spacing-md);
  font-weight: 500;
}

.project-link:hover {
  text-decoration: underline;
}

/* Certification styles */
.certification-entry .entry-issuer {
  color: var(--color-muted);
  font-size: var(--font-size-body);
}

.credential-id {
  font-size: var(--font-size-small);
  color: var(--color-muted);
  font-family: monospace;
}

.verification-link {
  display: inline-block;
  margin-top: var(--spacing-xs);
  font-size: var(--font-size-small);
  color: var(--color-accent);
  font-weight: 500;
}

/* Photo - Modern: rounded corners */
.photo {
  border-radius: 12px;
}

/* Theme toggle */
.theme-toggle {
  position: fixed;
  top: var(--spacing-md);
  right: var(--spacing-md);
  width: 44px;
  height: 44px;
  border: 2px solid var(--color-border);
  border-radius: 50%;
  background: var(--color-background);
  color: var(--color-body);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  transition: all 0.2s;
  z-index: 1000;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
}

.theme-toggle:hover {
  background: var(--color-surface);
  border-color: var(--color-accent);
}

.theme-toggle:focus {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}

.theme-icon {
  display: none;
}

[data-theme="system"] .theme-icon[data-icon="system"],
:root:not([data-theme]) .theme-icon[data-icon="system"] {
  display: inline;
}

[data-theme="light"] .theme-icon[data-icon="light"] {
  display: inline;
}

[data-theme="dark"] .theme-icon[data-icon="dark"] {
  display: inline;
}

/* Print styles */
@page {
  size: A4 portrait;
  margin: 0;
}

@media print {
  body {
    background: white;
  }

  .cv-page {
    box-shadow: none;
    margin: 0;
    width: 100%;
    min-height: auto;
  }

  .section {
    page-break-inside: avoid;
  }

  .entry {
    page-break-inside: avoid;
  }

  a[href]::after {
    content: none;
  }

  .theme-toggle {
    display: none !important;
  }
}
```

Modern template characteristics:
- Bold 28pt name with -0.5px letter spacing
- 3px accent-colored section dividers
- Accent-colored bullets
- Generous spacing (25mm margins, larger gaps)
- Tech tags with accent color text
- Highlighted projects with accent border + surface background
- 12px rounded corners for photo
  </action>
  <verify>
Verify file exists and contains Modern-specific styles:
- `grep "divider-width: 3px" templates/modern/styles.css`
- `grep "font-size-name: 28pt" templates/modern/styles.css`
- `grep "page-margin: 25mm" templates/modern/styles.css`
  </verify>
  <done>
Modern styles.css exists with bold geometric design: 3px accent dividers, 28pt name, 25mm margins, accent-colored bullets, rounded photo corners.
  </done>
</task>

</tasks>

<verification>
1. `ls templates/modern/` shows config.json, template.njk, styles.css
2. `cat templates/modern/config.json | jq .name` returns "Modern"
3. Modern template appears in `discoverTemplates()` output
4. Template renders correctly with all CV sections
5. CSS contains Modern-specific values: 3px dividers, 25mm margins, accent bullets
6. Dark mode toggle works in HTML output
</verification>

<success_criteria>
- Modern template directory exists with all three files
- config.json has correct name, description, and ATS compliance
- template.njk imports shared macros
- styles.css implements bold geometric design with accent dividers
- Template is ATS-compliant (single-column, semantic HTML, standard fonts)
- Template produces visually distinct output from base
</success_criteria>

<output>
After completion, create `.planning/phases/08-multi-template-polish/08-02-SUMMARY.md`
</output>
