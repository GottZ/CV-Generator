---
phase: 10-print-css-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/templates/src/render.ts
  - templates/base/styles.css
  - templates/modern/styles.css
  - templates/minimal/styles.css
  - templates/classic/styles.css
autonomous: true

must_haves:
  truths:
    - "Print CSS is loaded from _print.css, not duplicated in templates"
    - "Templates styles.css files contain no @page or @media print rules"
    - "CSS cascade order is correct: styleOverrides -> baseCss -> printCss"
  artifacts:
    - path: "packages/templates/src/render.ts"
      provides: "Print CSS loading from shared partial"
      contains: "_print.css"
    - path: "templates/base/styles.css"
      provides: "Screen-only template styles"
      not_contains: "@media print"
    - path: "templates/modern/styles.css"
      provides: "Screen-only template styles"
      not_contains: "@media print"
    - path: "templates/minimal/styles.css"
      provides: "Screen-only template styles"
      not_contains: "@media print"
    - path: "templates/classic/styles.css"
      provides: "Screen-only template styles"
      not_contains: "@media print"
  key_links:
    - from: "packages/templates/src/render.ts"
      to: "templates/_shared/partials/_print.css"
      via: "readFile and string concatenation"
      pattern: "_shared/partials/_print\\.css"
---

<objective>
Consolidate print CSS into a single source of truth by modifying render.ts to load _print.css and removing duplicated print CSS from all template styles.css files.

Purpose: Eliminate maintenance conflicts from having print CSS in 3 locations. Future print CSS changes will only need to modify _print.css.

Output: render.ts loads _print.css after template CSS; all 4 template styles.css files have print CSS removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-print-css-consolidation/10-RESEARCH.md

# Source files to modify
@packages/templates/src/render.ts
@templates/_shared/partials/_print.css
@templates/modern/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify render.ts to load _print.css after template CSS</name>
  <files>packages/templates/src/render.ts</files>
  <action>
Update the renderCV function to load _print.css and append it after baseCss:

1. Add import for `path` from 'node:path' if not present
2. In renderCV function, after loading baseCss, load print CSS:
   ```typescript
   // Load shared print CSS (single source of truth - PRINT-02)
   const printCssPath = path.join(templatesDir, '_shared/partials/_print.css');
   const printCss = await readFile(printCssPath, 'utf-8');
   ```
3. Update the css concatenation to include printCss at the end:
   ```typescript
   const css = `${styleOverrides}\n\n${baseCss}\n\n${printCss}`;
   ```

4. Apply the same changes to the createRenderer function (it has duplicate css loading logic).

The print CSS must come AFTER template CSS so it can override any conflicting rules (correct cascade order for CSS specificity).
  </action>
  <verify>
Run `grep -n "_print.css" packages/templates/src/render.ts` to confirm the import is added.
Run `bun run build` to verify the package compiles without errors.
  </verify>
  <done>
render.ts loads _print.css from _shared/partials/ and appends it after template CSS in both renderCV and createRenderer functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove print CSS from all template styles.css files</name>
  <files>
templates/base/styles.css
templates/modern/styles.css
templates/minimal/styles.css
templates/classic/styles.css
  </files>
  <action>
For each template's styles.css file, remove the @page rule and the entire @media print block at the end of the file:

**Pattern to remove from each file:**
```css
/* Print styles */
@page {
  size: A4 portrait;
  margin: 0;
}

@media print {
  body {
    background: white;
  }
  /* ... all rules through to closing brace ... */
}
```

**Line numbers from research (approximate, verify before editing):**
- base/styles.css: lines 469-529 (comment + @page + @media print block)
- modern/styles.css: lines 455-513
- minimal/styles.css: lines 455-513
- classic/styles.css: lines 454-512

After removal, ensure the file ends cleanly (no trailing empty lines beyond 1).

Do NOT remove any screen-only CSS (everything before the "Print styles" comment).
  </action>
  <verify>
Run: `grep -c "@media print" templates/*/styles.css` - should return 0 for all files.
Run: `grep -c "@page" templates/*/styles.css` - should return 0 for all files.
Run: `bun run build` to verify templates package still compiles.
  </verify>
  <done>
All 4 template styles.css files have no @page or @media print rules. Print CSS now comes only from _print.css loaded by render.ts.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "@media print" templates/*/styles.css` returns empty (no matches)
2. `grep -r "@page" templates/*/styles.css` returns empty (no matches)
3. `grep "_print.css" packages/templates/src/render.ts` shows the file is loaded
4. `bun run build` succeeds without errors
</verification>

<success_criteria>
- render.ts loads _print.css from _shared/partials/ directory
- All 4 template styles.css files have no print CSS
- Build succeeds without errors
- CSS cascade order is: styleOverrides -> baseCss -> printCss
</success_criteria>

<output>
After completion, create `.planning/phases/10-print-css-consolidation/10-01-SUMMARY.md`
</output>
