---
phase: 16-ai-content-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/ai/display/diff-display.ts
  - packages/cli/src/ai/display/quality-labels.ts
  - packages/cli/src/ai/utils/keyword-matcher.ts
  - packages/cli/package.json
autonomous: true

must_haves:
  truths:
    - "Side-by-side diff shown when terminal >= 120 columns"
    - "Inline diff shown when terminal < 120 columns"
    - "Quality labels use traffic light colors (green/yellow/red)"
    - "Fuzzy keyword matching works with Fuse.js"
    - "Exact keyword matching uses case-insensitive regex"
  artifacts:
    - path: "packages/cli/src/ai/display/diff-display.ts"
      provides: "Side-by-side and inline diff formatting"
      exports: ["displayComparison", "displaySideBySide", "displayInlineDiff"]
    - path: "packages/cli/src/ai/display/quality-labels.ts"
      provides: "Traffic light quality labels"
      exports: ["formatQualityLabel", "formatKeywordScore"]
    - path: "packages/cli/src/ai/utils/keyword-matcher.ts"
      provides: "Fuzzy and exact keyword matching"
      exports: ["matchKeywords", "KeywordMatch"]
  key_links:
    - from: "packages/cli/src/ai/display/diff-display.ts"
      to: "diff"
      via: "npm package for text comparison"
      pattern: "diffWords"
    - from: "packages/cli/src/ai/utils/keyword-matcher.ts"
      to: "fuse.js"
      via: "Fuzzy search library"
      pattern: "new Fuse"
---

<objective>
Create display utilities for diff comparison and quality indicators, plus keyword matching utility.

Purpose: The `ai improve` command needs diff display, all commands need quality labels, and `ai keywords` needs fuzzy/exact matching.

Output: Display and matching utilities ready for generator consumption.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-content-generation/16-CONTEXT.md
@.planning/phases/16-ai-content-generation/16-RESEARCH.md
@packages/cli/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install diff package and create diff display utility</name>
  <files>packages/cli/package.json, packages/cli/src/ai/display/diff-display.ts</files>
  <action>
Install the diff package:
```bash
cd packages/cli && bun add diff && bun add -d @types/diff
```

Create `packages/cli/src/ai/display/diff-display.ts`:

```typescript
/**
 * Side-by-side and inline diff display for content comparison.
 * Source: RESEARCH.md Pattern 6
 */

import { diffWords } from 'diff';
import pc from 'picocolors';

/**
 * Display comparison based on terminal width.
 * >= 120 columns: side-by-side
 * < 120 columns: inline diff
 */
export function displayComparison(
  original: string,
  improved: string,
  terminalWidth?: number
): string {
  const width = terminalWidth ?? getTerminalWidth();
  if (width >= 120) {
    return displaySideBySide(original, improved, width);
  }
  return displayInlineDiff(original, improved);
}

export function displaySideBySide(
  original: string,
  improved: string,
  width: number
): string {
  // Calculate column width (half of terminal minus separator)
  // Wrap text to fit columns
  // Format with headers: "Original" | "Improved"
  // Use dim separators
}

export function displayInlineDiff(
  original: string,
  improved: string
): string {
  // Use diffWords to get changes
  // Green + bold for additions
  // Red + strikethrough for removals
  // Plain for unchanged
}

function getTerminalWidth(): number {
  return process.stdout.columns || 80;
}

function wrapText(text: string, width: number): string[] {
  // Word-wrap text to fit within width
  // Return array of lines
}

function padRight(text: string, width: number): string {
  // Pad string to width with spaces
}
```

Update display/index.ts to include diff-display exports.
  </action>
  <verify>
Run `bun run typecheck` - should pass.
Run `bun run lint` - should pass.
Check `diff` is in package.json dependencies.
  </verify>
  <done>
displayComparison automatically chooses format based on terminal width.
Side-by-side shows Original | Improved columns.
Inline diff uses color-coded additions/removals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create quality labels utility with traffic light colors</name>
  <files>packages/cli/src/ai/display/quality-labels.ts</files>
  <action>
Create `packages/cli/src/ai/display/quality-labels.ts`:

```typescript
/**
 * Traffic light quality labels for generated content.
 * Source: RESEARCH.md "Quality Labels" code example
 */

import pc from 'picocolors';

export type Quality = 'strong' | 'good' | 'needs_review';

/**
 * Format quality label with appropriate color.
 * Strong = green, Good = yellow, Needs Review = red
 */
export function formatQualityLabel(quality: Quality): string {
  switch (quality) {
    case 'strong':
      return pc.green('Strong');
    case 'good':
      return pc.yellow('Good');
    case 'needs_review':
      return pc.red('Needs Review');
  }
}

/**
 * Format keyword coverage score with color.
 * >= 80% = green, >= 50% = yellow, < 50% = red
 */
export function formatKeywordScore(score: number): string {
  const percentage = Math.round(score);
  const color = percentage >= 80 ? pc.green
              : percentage >= 50 ? pc.yellow
              : pc.red;
  return color(`${percentage}%`);
}

/**
 * Format match score with color (for tailor command).
 * >= 75% = green, >= 50% = yellow, < 50% = red
 */
export function formatMatchScore(score: number): string {
  const percentage = Math.round(score);
  const color = percentage >= 75 ? pc.green
              : percentage >= 50 ? pc.yellow
              : pc.red;
  return color(`${percentage}%`);
}

/**
 * Format priority level.
 * High = red, Medium = yellow, Low = dim
 */
export function formatPriority(priority: 'high' | 'medium' | 'low'): string {
  switch (priority) {
    case 'high':
      return pc.red(pc.bold('High'));
    case 'medium':
      return pc.yellow('Medium');
    case 'low':
      return pc.dim('Low');
  }
}
```

Update display/index.ts to include quality-labels exports.
  </action>
  <verify>
Run `bun run typecheck` - should pass.
Run `bun run lint` - should pass.
  </verify>
  <done>
formatQualityLabel returns colored Strong/Good/Needs Review.
formatKeywordScore returns colored percentage.
formatMatchScore returns colored percentage with different thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create keyword matcher with fuzzy and exact modes</name>
  <files>packages/cli/src/ai/utils/keyword-matcher.ts</files>
  <action>
Create `packages/cli/src/ai/utils/keyword-matcher.ts`:

```typescript
/**
 * ATS keyword matching with fuzzy and exact modes.
 * Source: RESEARCH.md Pattern 3
 */

import Fuse from 'fuse.js';

export interface KeywordMatch {
  keyword: string;
  found: boolean;
  matchType: 'exact' | 'fuzzy' | 'not_found';
  matchedText?: string;
  section?: string;
  confidence: number;
}

export interface MatchKeywordsOptions {
  exact?: boolean; // Default: false (fuzzy matching)
}

export interface CVSection {
  name: string;
  content: string;
}

/**
 * Match job description keywords against CV content.
 */
export function matchKeywords(
  jobKeywords: string[],
  cvText: string,
  cvSections: CVSection[],
  options: MatchKeywordsOptions = {}
): KeywordMatch[] {
  if (options.exact) {
    return matchExact(jobKeywords, cvText, cvSections);
  }
  return matchFuzzy(jobKeywords, cvText, cvSections);
}

function matchExact(
  keywords: string[],
  cvText: string,
  sections: CVSection[]
): KeywordMatch[] {
  // Case-insensitive exact matching with word boundaries
  // Use regex: new RegExp(`\\b${escapeRegex(keyword)}\\b`, 'i')
}

function matchFuzzy(
  keywords: string[],
  cvText: string,
  sections: CVSection[]
): KeywordMatch[] {
  // Extract tokens from CV text
  // Create Fuse instance with threshold 0.3
  // Search for each keyword
  // Return matches with confidence scores
}

function extractTokens(text: string): string[] {
  // Split into words, normalize
}

function findSection(sections: CVSection[], text: string): string | undefined {
  // Find which section contains the matched text
}

function escapeRegex(string: string): string {
  return string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

// Common skill synonyms for matching
const SKILL_SYNONYMS: Record<string, string[]> = {
  'javascript': ['js', 'ecmascript', 'es6', 'es2015'],
  'typescript': ['ts'],
  'kubernetes': ['k8s'],
  'machine learning': ['ml'],
  'artificial intelligence': ['ai'],
  'project management': ['pm', 'program coordination'],
};
```

Update utils/index.ts to include keyword-matcher exports.
  </action>
  <verify>
Run `bun run typecheck` - should pass.
Run `bun run lint` - should pass.
  </verify>
  <done>
matchKeywords returns array of KeywordMatch with found status.
Exact mode uses case-insensitive word boundary matching.
Fuzzy mode uses Fuse.js with 0.3 threshold.
Results include confidence scores.
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run lint` passes
3. `diff` package is in package.json
4. All display utilities export through display/index.ts
5. All match utilities export through utils/index.ts
</verification>

<success_criteria>
- Diff display adapts to terminal width automatically
- Quality labels show correct colors for each level
- Keyword matcher supports both fuzzy and exact modes
- Fuse.js integration works with 0.3 threshold
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-content-generation/16-02-SUMMARY.md`
</output>
