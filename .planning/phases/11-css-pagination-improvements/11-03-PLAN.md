---
phase: 11-css-pagination-improvements
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - tests/pdf-modern.spec.ts-snapshots/modern-multi-page.png
  - tests/pdf-minimal.spec.ts-snapshots/minimal-multi-page.png
  - tests/pdf-classic.spec.ts-snapshots/classic-multi-page.png
autonomous: false

must_haves:
  truths:
    - "Visual regression tests pass with updated baselines"
    - "Multi-page PDF pagination shows improved page break placement"
    - "No entries are split across page boundaries (except long-entry)"
    - "Section headers appear with content, not orphaned at page bottom"
  artifacts:
    - path: "tests/pdf-modern.spec.ts-snapshots/modern-multi-page.png"
      provides: "Updated baseline for Modern template"
    - path: "tests/pdf-minimal.spec.ts-snapshots/minimal-multi-page.png"
      provides: "Updated baseline for Minimal template"
    - path: "tests/pdf-classic.spec.ts-snapshots/classic-multi-page.png"
      provides: "Updated baseline for Classic template"
  key_links:
    - from: "visual regression tests"
      to: "_print.css"
      via: "print media emulation"
      pattern: "toHaveScreenshot"
---

<objective>
Update visual regression baselines to reflect improved pagination and verify the changes work correctly across all templates.

Purpose: Ensure the pagination CSS changes produce the expected visual output and capture new baselines that reflect the improved page break behavior.

Output: Updated visual regression baseline snapshots for all three templates showing proper pagination.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-css-pagination-improvements/11-01-SUMMARY.md
@.planning/phases/11-css-pagination-improvements/11-02-SUMMARY.md
@tests/pdf-modern.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run visual regression tests to identify changes</name>
  <files>N/A (diagnostic)</files>
  <action>
Run the visual regression tests to see how the pagination CSS changes affect the output. The tests will fail because the baselines no longer match, but this is expected.

```bash
cd /workspace && docker compose run --rm test bun run test:visual 2>&1 | head -100
```

If Docker is not available or fails, run locally:
```bash
cd /workspace && bun run test:visual 2>&1 | head -100
```

Examine the test output to understand:
1. Which templates show differences
2. What the pixel difference percentage is
3. Whether the changes are improvements (better page breaks) or regressions

Expected outcome: Tests fail with differences in multi-page snapshots. Single-page snapshots should be unaffected (no pagination changes visible on single page).
  </action>
  <verify>
Test output shows failures for multi-page snapshots with pixel diff percentage.
  </verify>
  <done>
Visual regression test output analyzed, pagination changes confirmed to affect multi-page snapshots as expected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update visual regression baselines</name>
  <files>
tests/pdf-modern.spec.ts-snapshots/modern-multi-page.png
tests/pdf-minimal.spec.ts-snapshots/minimal-multi-page.png
tests/pdf-classic.spec.ts-snapshots/classic-multi-page.png
  </files>
  <action>
Update the baseline snapshots to capture the new pagination behavior:

```bash
cd /workspace && docker compose run --rm test bun run test:visual --update-snapshots
```

If Docker is not available:
```bash
cd /workspace && bun run test:visual --update-snapshots
```

This regenerates the baseline PNG files with the new pagination CSS applied.

After updating, verify the new baselines look correct:
```bash
ls -la tests/pdf-modern.spec.ts-snapshots/
ls -la tests/pdf-minimal.spec.ts-snapshots/
ls -la tests/pdf-classic.spec.ts-snapshots/
```
  </action>
  <verify>
Baseline snapshot files are updated (check modified timestamps).
  </verify>
  <done>
Visual regression baselines updated for all three templates with improved pagination rendering.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify tests pass with new baselines</name>
  <files>N/A (verification)</files>
  <action>
Run the visual regression tests again to confirm they pass with the updated baselines:

```bash
cd /workspace && docker compose run --rm test bun run test:visual
```

Or locally:
```bash
cd /workspace && bun run test:visual
```

All tests should pass. If any tests fail:
1. Check if it's a flaky rendering issue (re-run once)
2. If persistent, examine the diff images in test-results/
3. Adjust tolerance if needed (currently 1% threshold)
  </action>
  <verify>
All visual regression tests pass (exit code 0).
  </verify>
  <done>
Visual regression tests pass with updated baselines, confirming pagination CSS changes are captured correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
CSS pagination improvements that prevent entries from splitting across page breaks, convert flexbox to block for proper break property support, and add orphan/widow control for text elements. Baselines updated to capture new behavior.
  </what-built>
  <how-to-verify>
1. Generate a multi-page PDF:
   ```bash
   cd /workspace && bun run src/cli.ts generate tests/fixtures/multi-page-cv.md --template modern -o /tmp/pagination-test.pdf
   ```

2. Open `/tmp/pagination-test.pdf` and verify:
   - Job entries (Principal Software Architect, Senior Software Engineer, etc.) do NOT split across page breaks
   - Education entries remain intact on single pages
   - Section headers ("Experience", "Education", "Skills") appear WITH content, not orphaned at page bottom
   - No single isolated lines at top or bottom of pages
   - Page margins are consistent across all pages

3. Check for near-empty last page:
   - The PDF should not have a final page with only a tiny amount of content
   - Some whitespace at bottom of pages is acceptable for cleaner breaks

4. Optional: Test with other templates:
   ```bash
   cd /workspace && bun run src/cli.ts generate tests/fixtures/multi-page-cv.md --template minimal -o /tmp/pagination-minimal.pdf
   cd /workspace && bun run src/cli.ts generate tests/fixtures/multi-page-cv.md --template classic -o /tmp/pagination-classic.pdf
   ```
  </how-to-verify>
  <resume-signal>Type "approved" if pagination looks professional, or describe specific issues to address</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. All visual regression tests pass
2. Multi-page PDFs show proper pagination (no split entries)
3. Human verification confirms professional appearance
</verification>

<success_criteria>
1. Visual regression tests pass for all three templates
2. Baseline snapshots updated and committed
3. Human verifies multi-page PDF has no orphaned headers
4. Human verifies entries stay intact across page breaks
5. Human verifies no near-empty final pages
</success_criteria>

<output>
After completion, create `.planning/phases/11-css-pagination-improvements/11-03-SUMMARY.md`
</output>
