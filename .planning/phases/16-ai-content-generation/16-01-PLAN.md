---
phase: 16-ai-content-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/ai/utils/retry.ts
  - packages/cli/src/ai/utils/job-description.ts
  - packages/cli/src/ai/display/cv-format.ts
  - packages/cli/src/ai/utils/index.ts
  - packages/cli/src/ai/display/index.ts
autonomous: true

must_haves:
  truths:
    - "API errors are retried with exponential backoff (2-3 attempts)"
    - "Job descriptions can be loaded from file, URL, or stdin"
    - "Generated content outputs in cv.md format"
  artifacts:
    - path: "packages/cli/src/ai/utils/retry.ts"
      provides: "Retry with exponential backoff"
      exports: ["withRetry", "isRetryableError"]
    - path: "packages/cli/src/ai/utils/job-description.ts"
      provides: "Job description loading from multiple sources"
      exports: ["loadJobDescription", "JobDescriptionSource"]
    - path: "packages/cli/src/ai/display/cv-format.ts"
      provides: "cv.md output formatting"
      exports: ["formatBulletsAsCvMd", "formatSummaryAsCvMd"]
  key_links:
    - from: "packages/cli/src/ai/utils/job-description.ts"
      to: "packages/cli/src/ai/utils/retry.ts"
      via: "Uses retry for URL fetching"
      pattern: "withRetry"
---

<objective>
Create core utility infrastructure for Phase 16 AI content generation commands.

Purpose: Standalone generators need shared utilities for retry logic, job description loading, and cv.md output formatting that don't exist in Phase 15 workflow infrastructure.

Output: Three utility modules with barrel exports ready for generator consumption.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-content-generation/16-CONTEXT.md
@.planning/phases/16-ai-content-generation/16-RESEARCH.md
@packages/cli/src/ai/workflow/runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create retry utility with exponential backoff</name>
  <files>packages/cli/src/ai/utils/retry.ts, packages/cli/src/ai/utils/index.ts</files>
  <action>
Create `packages/cli/src/ai/utils/retry.ts`:

```typescript
/**
 * Retry utility with exponential backoff for API calls.
 * Source: RESEARCH.md Pattern 4
 */

export interface RetryOptions {
  maxAttempts?: number;  // Default: 3
  baseDelayMs?: number;  // Default: 1000
  maxDelayMs?: number;   // Default: 10000
}

export async function withRetry<T>(
  fn: () => Promise<T>,
  options: RetryOptions = {}
): Promise<T> {
  // Implementation from RESEARCH.md
  // - Loop through maxAttempts
  // - Catch errors, check isRetryableError
  // - Calculate delay with exponential backoff
  // - Sleep between retries
  // - Throw last error if all attempts fail
}

export function isRetryableError(error: Error): boolean {
  // Check for: rate limit, timeout, connection, ECONNREFUSED, 503, 429
}

function sleep(ms: number): Promise<void> {
  return new Promise(resolve => setTimeout(resolve, ms));
}
```

Create `packages/cli/src/ai/utils/index.ts` barrel export.
  </action>
  <verify>
Run `bun run typecheck` - should pass with no errors.
Run `bun run lint` - should pass.
  </verify>
  <done>
withRetry function exported and callable with async functions.
isRetryableError correctly identifies transient errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create job description loader for multiple sources</name>
  <files>packages/cli/src/ai/utils/job-description.ts</files>
  <action>
Create `packages/cli/src/ai/utils/job-description.ts`:

```typescript
/**
 * Load job descriptions from file, URL, or stdin.
 * Source: RESEARCH.md Pattern 5 and CONTEXT.md Section 4
 */

import { readFile } from 'node:fs/promises';
import path from 'node:path';
import { withRetry } from './retry.ts';

export interface JobDescriptionSource {
  type: 'file' | 'url' | 'stdin';
  content: string;
  metadata?: {
    filename?: string;
    url?: string;
    fetchedAt?: string;
  };
}

export interface LoadJobOptions {
  followRedirects?: number; // Default: 5
}

export async function loadJobDescription(
  source: string | undefined,
  options: LoadJobOptions = {}
): Promise<JobDescriptionSource> {
  // Detect source type:
  // - No source or '-' = stdin
  // - Starts with http:// or https:// = URL
  // - Otherwise = file path

  // For files: support .txt, .md, .pdf
  // For PDF: use unpdf for extraction, throw helpful error if fails
  // For URL: use withRetry, follow up to 5 redirects
  // Handle blocked sites with helpful error message per CONTEXT.md
}

async function readStdin(): Promise<string> {
  // Read from process.stdin until EOF
}

async function fetchJobFromUrl(
  url: string,
  maxRedirects: number
): Promise<JobDescriptionSource> {
  // Fetch with redirect handling
  // Extract text from HTML (simple stripping)
  // Return with metadata
}

async function extractPdfText(filePath: string): Promise<string | null> {
  // Try unpdf extraction
  // Return null if fails
}

function extractTextFromHtml(html: string): string {
  // Simple HTML to text extraction
  // Remove script/style tags, strip HTML entities
}
```

Update barrel export to include job-description.
  </action>
  <verify>
Run `bun run typecheck` - should pass.
Run `bun run lint` - should pass.
  </verify>
  <done>
loadJobDescription handles file, URL, and stdin sources.
PDF extraction attempted with helpful fallback message.
URL fetching includes redirect handling and blocked site detection.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cv.md output formatter</name>
  <files>packages/cli/src/ai/display/cv-format.ts, packages/cli/src/ai/display/index.ts</files>
  <action>
Create `packages/cli/src/ai/display/cv-format.ts`:

```typescript
/**
 * Format generated content as cv.md compatible output.
 * Source: RESEARCH.md "cv.md Output Formatting" example
 */

export interface RoleBullet {
  text: string;
  starBreakdown?: {
    situation: string;
    task: string;
    action: string;
    result: string;
  };
  metrics?: string[];
  quality?: 'strong' | 'good' | 'needs_review';
}

export interface RoleBullets {
  company: string;
  role: string;
  startDate: string;
  endDate: string | null;
  bullets: RoleBullet[];
}

export function formatBulletsAsCvMd(
  roles: RoleBullets[],
  locale: string,
  options?: { showStar?: boolean }
): string {
  // Format as cv.md work experience section
  // Include STAR as markdown comments if showStar
  // Example output:
  // ## Work Experience `en`
  //
  // ---
  // ### Role at Company
  // *StartDate - EndDate*
  //
  // - Bullet text
  // <!-- STAR: S: ... T: ... A: ... R: ... -->
}

export function formatSummaryAsCvMd(
  summary: string,
  locale: string
): string {
  // Format as cv.md summary section
  // ## Summary `locale`
  //
  // summary text
}

export function formatKeywordsAsCvMd(
  keywords: Array<{
    keyword: string;
    section: string;
    rewrittenContent: string;
  }>,
  locale: string
): string {
  // Format keyword placement suggestions
}
```

Create `packages/cli/src/ai/display/index.ts` barrel export.
  </action>
  <verify>
Run `bun run typecheck` - should pass.
Run `bun run lint` - should pass.
  </verify>
  <done>
formatBulletsAsCvMd produces valid cv.md experience section format.
formatSummaryAsCvMd produces valid cv.md summary section format.
STAR breakdown included as markdown comments when showStar=true.
  </done>
</task>

</tasks>

<verification>
1. `bun run typecheck` passes
2. `bun run lint` passes
3. All three utility modules exist and export their functions
4. Barrel exports in utils/index.ts and display/index.ts re-export all functions
</verification>

<success_criteria>
- withRetry retries failed API calls with exponential backoff
- loadJobDescription supports file paths, URLs, and stdin
- cv.md formatters produce output matching existing cv.md structure
- All modules have proper TypeScript types
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-content-generation/16-01-SUMMARY.md`
</output>
